# 複製成 .env 並填入實際值（.env 不會 commit 進 git）
#
# 選填：填了之後，i智慧 登入態過期時腳本會自動重新登入，不用人在旁邊手動打帳密。
# 不填也完全能用，只是過期當下要自己切到 Chrome 視窗手動登入一次。
ISMART_ACCOUNT=你的i智慧帳號
ISMART_PASSWORD=你的i智慧密碼

# 選填：每天 08:10 排程（daily_run.py）跑完要推 LINE 的 n8n webhook。
# 不填的話會自動改讀 scripts/keis/.env 的 KEIS_NOTIFY_WEBHOOK（同一條 n8n endpoint，
# 走它的 event=alert 分支），所以一般情況這裡什麼都不用填。
# BUYER_MATCH_NOTIFY_WEBHOOK=https://.../webhook/keis-grab
