# 事故紀錄與血淚史

> 這份放「已經處理完、但下次不看會再踩一次」的完整經過。
> 現況與待辦看 `STATUS.md`；查表看 `reference.md`。
> **新增事故一律加在最上面**，格式：日期 + 一句症狀 + 成因 + 怎麼修 + 學到什麼。

---

## 2026-08-14 全面拔掉主動 LINE 推播，改「工作回報」關鍵字查詢

**背景**：使用者要求重開 FB 廣告工作鏈（線 A/B/C），但 LINE 額度吃緊、不要任何主動推播，
全部改成打「工作回報」關鍵字才查、才回。

**動了什麼**：線 A（原本 7 個 push 節點）、線 B（1 個）、線 C（2 個）、系統錯誤 LINE 告警（1 個）
共 11 個推播點：
- 內容已經寫進 Notion 的（線 A 守門員擋下、發布成功、線 C 重發成功、被停確認）→ 直接刪掉節點，
  不留痕跡，因為 Notion 上本來就查得到
- 完全沒地方存的（API 體檢告警、跳過摘要、線 A 發文失敗、線 B 刪文失敗／KEIS 故障、線 C 守門員擋下、
  系統錯誤告警）→ 改寫進新建的 Notion DB「系統日誌」（`0df73bdfe72b4451bb841ebc151aecc9`）
- 新增 workflow「工作回報查詢」，LINE 打「工作回報」→ 查系統日誌今天的事件 ＋ 廣告 DB 今天異動 →
  組一則文字 reply 回去（reply 不吃 push 額度，跟戰果查詢/情資週報同一招）
- 線 A 的「10 分鐘煞車預告」push 也拔了（使用者確認不需要），`煞車 10 分鐘` Wait 節點本身沒動，
  「停」指令走 reply 一樣能用

**踩到的坑**：用 Claude 的 Notion 連接器建的新資料庫，n8n 自己的 Notion integration
（叫「n8n 廣告系統」）看不到——**不同 integration 各自的頁面存取權限是分開的，不會互通**，
新資料庫要手動到 Notion `Connections` 加一次才能用。以後要幫 n8n 建新 Notion 資料庫，
要嘛請使用者手動分享，要嘛用 n8n 自己的憑證跑一次「臨時 workflow 呼叫 Notion create-database」
（見 `reference.md` n8n 操作方式的試跑手法），不要用 Claude 自己的 Notion 連接器建。

---

## 2026-08-12～13 Codex 文案通道斷線，線 A 連續 5 班空跑失敗

**症狀**：n8n-live.md 顯示線 A 在 08-12 01:00／03:00、08-13 03:00／05:00／07:00 五個排定班次
全部 `error`，執行時間都只有 16～22 秒（很早就死，不是卡在慢步驟）。

**成因**：線 A「產文案」節點其實已經接到 `scripts/codex-copy`（本機服務透過 cloudflared 快速通道
把 Railway 上的 n8n 打回門市這台電腦），但 STATUS.md 當時還寫著「還沒接線」——**沒人更新過**，
所以沒人知道要盯著這個。cloudflared 快速通道本身不穩，`codex-copy` 的 log 顯示 08-12～13
反覆「通道掉了，重開並重新註冊」，好幾次重開失敗（cloudflared 自己結束了，通道沒開起來），
服務就那樣掛著、n8n 指向一個已經死掉的網址，直到下次有人手動重開 `server.py --tunnel`。

**學到什麼**：
1. **改了 n8n 節點但沒改 STATUS.md，等於沒人知道**——08-14 開工前 rebase 完看 STATUS 還是
   舊的，靠 `git diff` 比對 n8n_sync 拉回來的內容才發現網址跟上次不一樣，順藤摸瓜才查到。
   之後只要碰過的節點跟文件對不上，當場改，不要留到下一個 session。
2. **cloudflared 快速通道（無網域、免登入那種）本來就會斷**，`codex-copy` 有自動重開的邏輯，
   但重開本身也會失敗——這不是「掛了就會自己好」，開機自動啟動的捷徑（`啟動.vbs` 丟
   `shell:startup`）比手動盯著更可靠，08-12/13 全停時被移出後沒人放回去就是這次断線的根因之一。
3. 打 KEIS/Notion 那類外部服務斷線，`onError: continueRegularOutput` 那套救得回來；
   但這次是 n8n 自己的 HTTP 節點連不到「自己人」的服務，是網路層直接失敗，跟守門員或重試次數無關，
   查的時候不要往文案內容或守門員邏輯那邊找。

---

## 2026-08-10 差 7 秒打斷正在跑的班——deactivate/activate 不能在煞車窗口做

**經過**：19:10:20 為了收工把 n8n 產文案改回 Gemini，跑了 deactivate + activate。
19:00 那班（5958）當時正在「煞車 10 分鐘」裡面，19:10:27 才結束——**只差 7 秒**。
這次它跑完了（AG1918071 有發出去），但那是運氣。

**學到什麼**：掃描發文線 09/11/13/15/17/19 的 **:00～:10 是煞車窗口**，執行還掛在那裡。
CLAUDE.md 只寫「改完要 deactivate + activate」，沒寫「什麼時候不能做」。
**整點後 10 分鐘內不要動 n8n 的啟用狀態**，要動就等 :15 之後。
`codex-copy` 的 `--tunnel` 模式是開機時（約 07:30）註冊，剛好避開所有窗口。

---

## 2026-08-10 守門員第 10 個誤擋：中文數字沒換算（＋一次沒查出根因的佇列卡死）

**症狀**：15:00 那班（execution 5919）AG1918052／AG1918071 兩件雙店面被擋，理由「查無出處的數字：10」。

**成因**：KEIS 賣點原文寫「衛武營**十**分鐘車程」，AI 產文案時改寫成「**10** 分鐘」。守門員拿「10」
去原文找，原文只有中文的「十」，判定 AI 編造。**跟前面 6 次是同一個病**：AI 會改寫數字的「寫法」
（全形→半形、元→萬、7-ELEVEN→7-11、民國日期、千分位逗號、中文數字），守門員只會逐字比對。

**怎麼修**（PR 見 commit）：
1. 治標——守門員把原文的中文數字換算後也放進事實庫。**只在數字後面接量詞時才換算**
   （分鐘/坪/房/樓/米/年/元…），否則「三多商圈」「四維路」「十全路」這類路名會被當成數字放行，
   等於偷偷放寬檢查。13 個測試案例（含 5 個路名）全通過。
2. 治本——`Gemini 產文案` 指令加「【數字寫法】賣點素材裡的數字一律照原文寫法照抄，不得換算」，
   把問題堵在源頭，不用永遠追第 11、12 個 bug。
3. AG1918052／AG1918071 改名 `-已重跑` + 狀態改取消，讓下一班重新抓到。

**驗證**：拿 5919 的真實資料重跑改過的守門員，3 件全放行；另外做回歸測試——賣點只有「十分鐘」、
文案寫「步行8分鐘」時**仍然被擋**（bad=["8"]），確認沒把 AG1869334 那個洞打開。

**學到什麼**：
- 誤擋率算出來是 **11:1**（3 週 94 篇發布、11 筆誤擋後重跑、只有 1 筆 AG1869334 擋對）。
  補第 N 個特例不是解法，要往「不讓 AI 改寫數字」的方向堵。
- **修守門員時只能「補真出處」，絕不能「放寬檢查」**。差別在：把「十分鐘」換算成 10 是補出處
  （原文真的有這個事實）；把「凡是分鐘都放行」是拆掉驗證（AG1869334 就是這樣抓到的）。

**同一天另一件、根因沒查出來**：11:00 那班（5877）跑 852 秒後被 Bull 佇列判定
`job stalled more than maxStalledCount` 放棄，同時 n8n 對外間歇性 502。已排除：磁碟滿
（Postgres 才 4.2%）、n8n 重開機（心跳每 10 分鐘照跑沒斷）、流量突增。**程序活著但偶爾幾秒沒反應，
之後 3 班（627/652/627 秒）全正常，自己好了。** n8n retry 對這種佇列卡死無效（32ms 就回同樣錯誤）。
兩個體質問題順帶查到但還沒修：n8n 內建 insights 統計 08-02 起就壞了還在狂報錯洗版 log、
照片走資料庫沒設 `N8N_DEFAULT_BINARY_DATA_MODE=filesystem`（binary_data 表已 169MB）。

---

## 貫穿全部事故的三句話

1. **流程回報成功 ≠ 事情做到了。** FB 回 500 貼文其實發出去了、n8n 執行 success 但節點錯誤被吞掉、
   LINE 回 500 通知沒送到——判斷「寫入到底成功沒」不要只看有沒有噴錯。
2. **保底邏輯只要「讓流程繼續」而不管「送出去的東西對不對」，它就是在把故障洗成看起來成功的樣子。**
   對外送出的東西一律要有形狀檢查。
3. **改完沒有在「真正在跑的環境」實地驗一次 = 沒改。** 補防護一律要有一次可觀察的實測
   （例如真的收到一則 LINE），不能只看程式碼寫對了。

---

## 2026-08-09 ─ Postgres volume 撐到 100%，n8n 全停約 1.5 小時（已修）

### 🔴 結論先寫：清 n8n 執行紀錄一律用 TRUNCATE，**絕對不要用 API 批次 DELETE**

Postgres 的 DELETE 會先寫交易日誌（WAL）才釋放空間。在一個已經 86% 滿的磁碟上批次刪
519 筆大檔紀錄，等於一邊要空間一邊先灌資料進去——**當場把剩下的 14% 吃光**，
Postgres 連啟動時寫 WAL 的空間都沒有，直接停機。

**症狀**：n8n API 全部 503；Railway 上 Postgres `Crashed`；volume 顯示 100%。
Deploy Logs 裡的關鍵句是
`FATAL: could not write to file "pg_wal/xlogtemp.26": No space left on device`
——每次重啟都跑完 32 秒資料復原、在最後寫入那步自殺，**全程不開放連線**，
所以「等它起來的瞬間搶進去清」這招不可能成功。

### 真正的成因（跟停機是兩件事，要分開看）

`靜默失敗巡邏` 每 30 分鐘抓一批執行紀錄的**完整內容**（`?includeData=true`，含廣告照片），
而全域 `SAVE_ON_SUCCESS=all` 又把它抓回來的這包大資料原封不動存回它自己的執行紀錄裡。
**等於每 30 分鐘把別人已經存過的照片再複製一份**，單次執行紀錄約 50 MB。

- 2026-08-02 量測：volume 914 MB（18%）
- 2026-08-09 量測：4.3 GB（86%），`execution_data` 單表 3510 MB
- 7 天漲 3.4 GB ≒ 每天 480 MB，**跟業務量、廣告則數完全無關**

修法是把**這一支** workflow 的 `saveDataSuccessExecution` 設成 `none`
（workflow 層級，不是全域——全域那條紅線沒動）。它本來就不需要回看自己的執行紀錄，
也沒有 Wait 節點，關掉零副作用。

### 怎麼救回來的（Hobby 方案，volume 加不了、備份是 Pro 限定）

1. Railway → Postgres → Settings → Custom Start Command 暫時改 `sleep infinity`
   （原值 `/bin/sh -c "unset PGPORT; docker-entrypoint.sh postgres --port=5432"`），
   容器才會停止自殺迴圈、SSH 進得去
2. `railway ssh --service Postgres`，把整個 `pg_wal`（721 MB）搬到容器暫存碟
   （`/` 有 523 GB 可用），原位置放符號連結接回去。
   **Postgres 官方支援 pg_wal 是 symlink**（`initdb --waldir` 就是這樣），不刪任何檔案、可逆
3. 用 **5433 埠**啟動（`pg_ctl -o '-p 5433'`）——避開 n8n Primary/Worker 一連上就開始寫
4. `TRUNCATE TABLE public.execution_entity CASCADE;` + `CHECKPOINT`
   → volume 100% → 5%，**資料一筆沒少**（redo 完整跑完）
5. 乾淨關閉 → **把 pg_wal 搬回 volume** → 最後才還原 Custom Start Command
   ⚠️ 順序不可顛倒：還原啟動指令會重建容器、暫存碟全毀，pg_wal 還在那邊就完蛋

### 學到什麼

- **不要在快滿的磁碟上做任何「先寫後放」的操作**。DELETE、VACUUM FULL、REINDEX 都是。
  TRUNCATE 是直接 unlink 檔案，才是對的工具
- **Hobby 方案沒有安全網**：volume 鎖死 5 GB 加不了，備份與還原是 Pro 限定。
  唯一的那份備份（495 MB，7/31 resize 時自動產生）點下去是鎖頭
- **當時完全沒有東西會叫**，是人工看 Railway 帳單才順手發現的。已補
  `ad_watchdog.py` 判斷四：Postgres 佔 volume 超過 70% 就繞過 n8n 直推 LINE
- 帳單本身不是問題：$7.14 裡 $7 是四個容器 24 小時開機的記憶體底薪，
  跟跑幾次 workflow 無關（vCPU 只花 $0.10）

## 2026-08-08 ─ KEIS 公買搶單：Notion 補寫路徑會把備註清空，靜靜漏了至少 5 筆（已修）

**症狀**：使用者發現 79968（KEIS 有備註「問專員電話」）Notion 頁面備註是空的。跑修復機制
一驗，又抓到 82348、81814、80026、79967 同樣被漏——不是單一個案。

**成因**：`push_notion()` 第一次因 Notion API 暫時性錯誤（ConnectError/逾時）失敗時，記進
`notion_pending.txt` 靠 `reconcile_notion()`／`audit_notion()` 之後補寫。補寫用
`load_grabbed_row()` 從本機 `grabbed.csv` 重建資料，但 `grabbed.csv` 從沒存過備註欄，
重建時 `remarks` 寫死空字串——備註只要走到補寫這條路一律消失，第一次直接成功寫入才會有。

**怎麼修**（`scripts/keis/grab.py`，PR #175／#177）：
1. `grabbed.csv` 加「備註」欄，補寫路徑重建資料時讀得到（堵住「這一種」漏法）
2. 新增 `audit_remarks()`：每天跨日結算時跟各帳號『我的申請』API 現況對帳（不信任本機快照）
   ——KEIS 現在有備註、Notion 是空的就直接補上，不管未來是什麼新原因造成的都抓得到。
   有 `--audit-remarks` CLI 可手動跑；已跑過一次證實抓到上述 4 筆歷史漏寫。

**學到什麼**：「已知失敗路徑重建資料」這招（`load_grabbed_row` 這類）**重建欄位要跟原始寫入
欄位一一對齊**，少存一欄就是補寫必漏一欄，而且是那種不會噴錯、只會安靜變空白的漏法。
比對「單一快照重建」更耐打的做法是跟上游系統（這裡是 KEIS）現況對帳，不管什麼原因造成
的落差都會被抓到，不用等使用者自己發現。

---

## 2026-08-08 凌晨 ─ 第 6 個誤判：AI 把「元」換算成「萬」，數字守門員比不到出處（已修）

**症狀**：01:00 那班 EG0501418（三多商圈透店）被擋「查無出處的數字：4.5」，文案寫「現收租
4.5萬/月」。

**成因**：賣點原文是「現收租45,000每月」，AI 換算成慣用的「4.5萬/月」寫法（45000÷10000=4.5，
算對了），但 `數字守門員` 只認逐字比對，事實庫裡從沒出現過「4.5」這個 token，判「查無出處」。
另外賣點原文的千分位逗號「45,000」在抓數字的 regex 眼中會被切成「45」+「000」兩截，「45000」
這個完整數字根本沒進過事實庫，連轉都沒東西可轉。

**怎麼修**（`workflows/yc-v3-scan-publish.json` 的 `數字守門員` 節點）：
1. 抓數字前先去掉千分位逗號（`(\d),(?=\d{3}(?:\D|$))` → 去掉逗號），「45,000」還原成「45000」
2. 整數 ≥1000 時，額外把它除以 10000 的結果也算進事實庫（元轉萬也算有出處）

用 node 直接跑這段 JS 邏輯測過：EG0501418 真實資料轉 OK；合成「AI 憑空編 8 分鐘」「編 9.9 分」
兩個亂編數字案例還是照樣被擋，沒有把守門員鬆到抓不到真編造。已用 Public API PUT 推上 n8n
並 deactivate+activate。EG0501418 在 Notion 已改名 `-已重跑` + 狀態改「取消」，等下一班重新撈。

**學到什麼**：AI 對數字做「單位換算」（元↔萬、坪↔平方公尺這類）是合理改寫，跟「憑空編數字」
（AG1869334 的步行時間）不是同一類，事實比對要能吸收常見換算，不能無限上綱到「文案只准照抄」。

---

## 2026-08-07 晚 ─ Notion 擋下列漏寫理由，害我們把一筆誤擋判成「AI 編錯」（已修）

**症狀**：EG0530884（大樹九曲路乙工廠房）被守門員擋下，Notion 上的理由是**空白**
（`⚠ 守門員擋下 2026-08-07：｜文案已產好…`）。人只好自己讀文案猜，猜成「AI 編錯格局」，
於是被排除在那批重跑名單之外，多躺了一天。

**成因**：`Notion 建擋下列` 節點組理由時只帶了兩種：
```js
[...(_guardBanned||[]), ...(_guardBad||[]).map(n=>'查無出處數字 '+n)].join('、')
```
但 `數字守門員` 其實會產四種：`_guardBanned`（欄位不准提）、`_guardBad`（查無出處數字）、
`_guardJunk`（形狀壞掉）、`_guardReview`（交叉審核 NG）。**後兩種從來沒寫進 Notion**——
被交叉審核擋下的案子，Notion 上永遠是空白理由。LINE 告警那則有完整理由，只有 Notion 漏。

**怎麼修**：把四種都串進去，全空時寫「（理由沒帶到，看 LINE 守門員告警那則）」。
已 PUT 上線 + deactivate/activate 一次。

**使用者的實質糾正（比 bug 本身重要）**：
- **廠房／土地「格局空白」是正常的**，開放空間本來就沒有房間，不是資料缺漏、更不是 AI 寫錯
- 廠高 13 米、前臨 8 米路**本來就不是 KEIS 欄位資訊**，不能因為欄位沒有就當成編造
- 而守門員其實**沒有**擋這兩個數字（Notion 理由裡沒有「查無出處數字 13／8」），
  代表它們在賣點原文裡找得到。**不要為了這件事去放寬守門員**——它這次是對的，錯的是回報

**學到什麼**：**擋下理由是人唯一的判斷依據，漏一種就等於逼人用猜的。**
機械檢查回報不完整時，人會拿文案自己腦補一個成因，而且會很有信心地寫進交接文件。
對照組：AG1869334 的「查無出處數字 8」＝文案自己編的「步行 8 分鐘」，賣點原文沒有，
守門員抓對了——同一天、同樣是「8」，一個該擋一個不該擋，差別只有理由寫不寫得出來。

---

## 2026-08-07 ─ 兩天內 4 件廣告被誤擋，2 個獨立 bug（已修）

**症狀**：使用者反映 08-06、08-07 連續有廣告被攔截。查 n8n executions 抓到 4 件：

⚠️ 下面時間全部是**台灣時間**（n8n 執行紀錄本身存的是 UTC，查資料時忘記 +8，事後才發現整批寫錯，已訂正）。

| 時間 | 物件 | 擋的機制 | 原因 |
|---|---|---|---|
| 08-06 11:00 | YG0137742 | 數字守門員 | 查無出處的數字「113」——KEIS 賣點原文寫的是全形「１１３年更換電梯」，AI 產文案轉半形「113」，比對用的正則只認半形，兩邊對不上 |
| 08-06 11:00 | AG1863917 | 數字守門員 | 不該提「學區」——案名本身叫「鳳翔**學區**/全新最強2+1房」，AI 把案名帶進標題，學區欄位卻是空的 |
| 08-07 11:00 | EG0530884 | 文案交叉審核（08-06才上線） | 格局寫「透天廠房」，官方格局欄位是空的、賣點原文完全沒提——**這件是真的誤擋，AI 憑空編造，機制抓對了** |
| 08-07 13:00 | AA1044536 | 文案交叉審核 | verdict=NG，但 reason 原文自己寫「這屬於官方資料矛盾，不應由我判斷為NG。我應放行」——AI 自己推翻了自己的判定，verdict 欄位卻沒跟著改 |

**AA1044536 的成因**：審核 prompt 要求回傳 `{"verdict":"OK或NG","reason":"..."}`，verdict 排在 reason **前面**。
`文案交叉審核` 節點用的是 `gemini-2.5-flash` 且 `thinkingConfig.thinkingBudget: 0`（關閉思考，2026-08-04 那次 output token 被思考吃光的教訓），
模型必須逐字直接生成 JSON、沒有「先想後答」的空間，於是先吐出 verdict 才想清楚，reason 寫到一半想通了却已經來不及改前面的 verdict。

**YG0137742 的成因**：`數字守門員` 建事實庫時用 `raw.match(/\d+(?:\.\d+)?/g)` 只認半形阿拉伯數字，KEIS 原始賣點文字裡的全形數字（１１３）完全掃不到，
所以「113」這個數字永遠進不了 `facts` 集合——即使它明明就寫在官方賣點原文裡。

**怎麼修**（3 個都推上線了，`yc-v3-scan-publish` 已 deactivate+activate）：
1. `文案交叉審核` prompt 改成 reason 放在 verdict **前面**，並加一句「reason 裡出現『應該放行/不該擋』，verdict 就必須填 OK」，強制先想清楚再下結論
2. `Gemini 產文案` prompt 加一句：學區欄位空的時候文案全文（含標題）不准出現「學區」兩字，**就算案名/社區名稱本身帶學區字樣也要避開**——不是修守門員放寬標準，是從源頭不讓 AI 寫
3. `數字守門員` 加 `toHalf()` 把全形數字轉半形再比對，KEIS 原文裡的全形數字現在算數了

**沒有動的**：EG0530884 那件——AI 真的憑空編造格局，交叉審核抓對了，不是 bug。

**後續（同一天稍晚）**：2026-07-30 診斷出的 3 個守門員誤判也一起修完了，見下一則。

**學到什麼**：LLM 結構化輸出的欄位順序不是排版問題——關掉思考預算（thinkingBudget:0）的模型是逐字生成，
「先問結論、後問理由」的 schema 會逼它來不及反悔。這類審核 prompt 一律要 reason-before-verdict。
另外：任何「跟官方文字比對」的檢查，都要想到 KEIS 原始資料可能用全形數字/標點，程式這邊只認半形是常見漏洞。

---

## 2026-08-05 ─ 下架偵測的「二次確認」早就恆真：`來源連結` 存的是會過期的 houseol 網址

**怎麼發現的**：使用者問「Notion 的來源連結是幹嘛的，網址點進去都沒內容」。實測 77 筆裡 72 筆是
houseol（`sell_item/H888-Sxxxxxxx`），**連當天新建的那筆都回 404**，頁面寫「抱歉您可能晚一步…或連結失效了」。

**成因**：這欄是 v1/v2「LINE 貼永慶網址建檔」時代的**輸入兼判重鍵**（commit `0b7eeda`，理由：案件編號
格式不穩、案名會撞）。v3 改成掃 KEIS API 後，輸入來源換成 KEIS 物件的 `official_url`——那是 houseol，
而 KEIS 149 個欄位裡沒有任何永慶 product id（工單 15.2 查證過）。**同一個欄位換了資料來源、名字和
用途沒改**，欄位說明還寫著「buy.yungching.com.tw 物件網址」，跟內容打架好幾週沒人發現。

**真正的風險**（不是網址醜而已）：線 B 對「沒有 `KEIS廣告ID` 的 v2 舊資料」的雙重確認是
「KEIS 查無 **而且** 連結死了」。houseol 全部 404 ⇒ 第二個條件恆真 ⇒ **保險絲等於沒接**。
當時有 3 筆「已發布 + 無 KEIS廣告ID」的物件（PA0056433、BG0211984、AG1927914）houseol 404 但
永慶官網連結還活著（實測 200），只要哪天 KEIS 物件 API 抓不到就會被誤判下架、粉專貼文被自動刪掉。

**怎麼修**：欄位刪掉，所有引用改指 `永慶官網連結`（線 A 用物件編號反查、KEIS 廣告也是拿它當
`adcase_url`）。動的地方：線 B 二次確認、線 C 的死碼（讀進來從沒被讀第二次）、線 A 停寫 +
`API 體檢` 移除 `official_url`、`publish.py`、yc-ad skill、停用的建檔器 v2/v3 與 v2 發文線。
刪之前先把 4 筆 v2 舊資料的永慶網址搬進 `永慶官網連結`（其中 YC1868705 還在架上）。
houseol 網址沒有備份也不需要：KEIS 的 `official_url` 隨時撈得回來。

**學到什麼**：**欄位換了資料來源，就要換名字或當場改掉欄位說明。**
名字沿用舊語意、內容換成新東西，是最難發現的一種壞——它不會報錯，只會讓依賴它的判斷靜靜失效。
另外：只要有「兩個條件都成立才動手」的保險，就要定期驗那兩個條件**是不是還有分辨力**。

---

## 2026-07-30 ─ 守門員誤擋：4 班擋了 4 件，其中 3 件是誤判（**2026-08-07 已修**）

**症狀**：當天 09:00 ✅、11:00 ✅、13:00 一件被擋→自動換下一件發出、15:00 三件全沒過→沒發。
**FB API 沒拒絕過、n8n 0 錯誤**，全部是自己攔的。診斷完成、修法未實作，待辦一行留在 STATUS.md。

| 物件 | 系統說 | 真相 | 要改哪裡 |
|---|---|---|---|
| EG0501404 鳳山紫金城 | 不該提「學區」 | 文案寫的是「O14鳳山國中**捷運站**」，站名不是學區 | 判學區前先排除捷運站名，或改成只有出現「學區/就讀/劃分」等語境詞才算宣稱 |
| YG0134508 岡山鳳凰信義 | 不該提「樓層」 | KEIS 寫 `14-13/14`、官網寫 `13/14` → 交叉檢查判不一致 → 樓層被清空 → 文案有「次高樓層」就被擋 | 不一致時改**採用官網值**（公開可驗證）而不是清空；或放行不含數字的相對描述 |
| EG0529474 榮總時代爵邸 | 官網查不到連結 | **官網有**：`house/7457619` productID `TC0529474` 完全對上。當班抓到的是空殼頁（名/社/路/價全 X＝內容沒載到），因為那是當天剛上架的新頁 | ①候選頁全讀不到 productID 時當「抓失敗」重試，不要當「不是這件」 ②`if (!cands.length && community)` 改成「編號搜尋的候選全比不上時也退回社區名搜尋」——當天搜「時代爵邸」21 筆裡就有正確那筆 |

**學到什麼**：守門員擋下來的東西要留下「擋什麼、為什麼」，不然只會看到「今天沒發文」。
兩件被擋的已在 Notion（狀態=`草稿`、文案都產好了、備註寫原因），要發就改成「待發」；
**系統不會自己再試那兩件**。EG0529474 那件沒建 Notion，每班都會自動再試。

### 2026-08-07 補：3 件全部修完

- **EG0501404（捷運站誤判學區）**：`數字守門員` 的學區 regex 加負向 lookahead，緊接著是「站」或「捷運站」的不算學區宣稱
- **YG0134508（樓層不一致被清空）**：`永慶官網補資料` 不一致時改**採用官網值**，格局也一併套用同樣邏輯（原本只有樓層被點名，格局是同一種病，一起治）
- **EG0529474（空殼頁比不中）**：沒有實作「偵測空殼頁重試」（沒有實際空殼頁樣本，怕猜錯 heuristic 反而多打一堆無效請求）。改用範圍更廣的解法：**編號搜尋候選全部比不中時（不只候選是空的時候），一律加試一輪社區名搜尋**——這樣不管原因是空殼頁還是別的，只要正確頁面在社區名搜尋的候選裡就撈得到，`時代爵邸` 21 筆那次會被涵蓋

三處都用真實資料/情境跑過（YG0137742 全形數字、EG0501404 捷運站字串）確認行為對，`yc-v3-scan-publish` 已 deactivate+activate。

### 2026-08-07 再補：清 Notion 舊草稿時又抓到第 4 個誤判（已修）——「7-11」被當成數字 11

盤點 8 筆狀態=草稿的舊資料時發現 AA1076686（07-28）被擋「查無出處數字 11」，查賣點原文是「7-ELEVEN」，
AI 改寫成慣用的「7-11」，`\d+` 正則把 "7" 和 "11" 拆成兩個數字，"11" 在事實庫裡找不到出處。
修法：掃描文案數字前，先把「7-11」換回「7-ELEVEN」再比對。真實案例＋合成的「真的亂編數字」案例都測過。

### 2026-08-07 再補：第 5 個誤判——民國日期「117.04.24」月份掉出事實庫

09:00 那班 EG0501418 被擋「查無出處的數字：4」，使用者確認「這是建物現況有出租，只是描述實際租賃金額」，
查賣點原文是「租期至117.04.24」。`\d+(?:\.\d+)?` 這個正則貪婪比對，把「117.04.24」吃成「117.04」＋「24」
兩個 token，月份「04」從沒單獨進過事實庫。文案把日期寫成人話「117年4月24日」時，單獨的「4」（月份）
就找不到出處。修法：另外用 `\d{1,4}\.\d{1,2}\.\d{1,2}` 抓出日期格式的三段式數字，三段各自也算數字出處。
真實資料＋合成的亂編數字案例都測過。

---

## 2026-08-04 ─ 下架偵測對沒貼文的舊資料照樣打 FB DELETE，被靜默失敗巡邏抓到

**症狀**：靜默失敗巡邏 LINE 告警：下架偵測線 `FB 刪貼文`（執行 #4887）回
`Unsupported delete request`，但整條執行狀態仍是「成功」（節點設了 `neverError`）。

**查證**：命中的是 YE0095535（`其他待辦` 早就記著的 3 筆 v2 舊資料之一，Notion 沒存
`粉專貼文連結`，`KEIS廣告ID` 也是空的）。那天它的 houseol 來源連結終於回 404，觸發下架判定。
Notion／KEIS 三邊事後核對**沒有壞資料**：Notion 正確標成「下架」，KEIS 本來就沒有這筆廣告，
沒有任何貼文被誤刪或誤留。

**成因**：`備妥刪文` 不管有沒有貼文連結，一律接去 `FB 刪貼文`。沒有 `fbPostId` 時
URL 變成 `graph.facebook.com/v21.0/`（空 ID），FB 當然回 `Unsupported delete request`。
下游 `判定刪除結果` 靠 `hasFbPost` 判斷要不要理會這個回應，所以結果沒錯，
但**每次處理沒有貼文的舊資料，都會白打一次注定失敗的 API、噴一次會被巡邏抓到的錯誤**——
不是資料事故，是雜訊源，放著不管會讓「靜默失敗巡邏」的訊噪比越來越差。

**怎麼修**：`yc-v3-removal.json` 加一個 IF 節點「有貼文可刪?」，`hasFbPost` 為否時直接跳過
`FB 刪貼文`、接去 `判定刪除結果`（該節點在無貼文分支本來就不讀 FB 回應，繞過去不影響邏輯）。
兩條分支各寫一版隔離測試驗證過（無貼文＝直接判定完成、有貼文＝迴歸正常），已部署
（deactivate+activate 過）。

**學到什麼**：`neverError` + 下游自己判斷成功失敗，是刻意的設計（單筆失敗不能拖垮整批），
但**不代表上游可以隨便亂打注定失敗的請求**——已知會失敗的呼叫要在源頭擋掉，不要靠下游
「反正看得懂這個錯誤」來擦屁股，不然巡邏機制遲早會被這類已知噪音淹沒，真正的新錯誤反而不顯眼。

---

## 2026-08-04 ─ 撞號比對到別間房子，樓層資料洩漏進已發布的土地廣告

**症狀**：使用者在 KEIS 廣告追蹤發現幾筆「官網連結失效」，順藤摸瓜找到 LA0067094
（大樹區 800坪土地，4800萬）的官網連結指到新北淡水的一間電梯大樓，而且**已經發出去的粉專貼文**
規格區寫著「🏢10/13」——一塊地寫樓層，是廣告不實。

**成因**：LA0067094 是 07-30 建的，那時 `永慶官網補資料` 只看「編號數字對上」就採用，
沒有行政區/坪數佐證。搜尋結果第 2 筆剛好是新北淡水 YE0067094（編號數字撞號），
比對命中錯的物件，該物件的樓層「10/13樓」被寫回 `src.樓層`，跟著進了 Gemini prompt，
直接印在發出去的文案上。08-01 已經加過「撞號防線」（行政區＋總價/坪數佐證）修過這個比對邏輯本身，
**但沒有回頭救這一筆已經產生、已經發布的舊資料**——防線只擋新案子，舊資料不會自動變乾淨。

**另一層漏洞**：就算比對邏輯完全正確，土地／車位本來就沒有「樓層」這個屬性，
`永慶官網補資料` 卻沒有依 `建物類型` 排除，任何配對到的官網頁只要抓得到樓層字樣就會照填，
這是即使配對正確也可能發生的獨立風險。

**怎麼修**：
1. 重新用現有的撞號防線邏輯手動比對 LA0067094，找到正確頁面
   `buy.yungching.com.tw/house/7357184`（TC0067094，行政區/總價/地坪/地段全部對上）。
2. 用臨時 workflow 借 FB Page Token，把已發布貼文的「🏢10/13」那行拿掉，改回乾淨版本。
3. Notion `粉專文案` 同步改正。
4. KEIS 廣告追蹤 `adcase_url` 改成正確頁面（`PUT /adcases/{id} {adcase_url}`），
   `url_invalid` 立刻變 `false`；`降價3702萬` 那個 status_tags 是 KEIS 自己排程算的，會在下次爬蟲跑完自動消掉。
5. **根治**：`永慶官網補資料`（`yc-v3-scan-publish.json` / `yc-v3-repost.json` 共用）加一道
   `isLand = /土地|農|廠|車位/.test(建物類型)` 檢查，土地/車位案不管官網比對到什麼，
   一律不把樓層/格局填回去。4 案例迴歸測試通過（土地案被擋、一般大樓案不受影響），已部署
   （deactivate+activate 過）。

**學到什麼**：
1. **修比對邏輯只保護未來的新資料，不會回頭清洗已經產生的舊資料**——這類 bug 要主動去查
   「這個邏輯上線之前建的案子」還有沒有殘留，不能假設修完就沒事了。
2. **「查得到就填」不等於「填了合理」**：光靠「有沒有比對成功」判斷該不該用一個欄位不夠，
   物件類型本身能不能有這個屬性，要單獨查，不能靠比對邏輯間接擋掉。

---

## 2026-08-04 ─ 測試打爆 Gemini 每日配額，弄掛 11:00 那班廣告

**症狀**：11:00 班（execution 4725）在 `Gemini 產文案` 失敗，訊息
`The service is receiving too many requests from you`。

**成因**：我（Claude）在開發「文案交叉審核」時，用臨時 webhook workflow 連續打了約 50 次 Gemini，
把當天的配額吃光。**這支金鑰是免費方案，`gemini-2.5-flash-lite` 每天只有 20 次**
（`quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier`、`quotaValue: 20`），
而且是每日配額不是每分鐘——訊息長得像「打太快」，實際上是「今天用完了」。

**災損**：只掉一班廣告。失敗點在 `Gemini 產文案`，**在建 Notion 待發列和發 FB 之前**，
所以 Notion／FB／KEIS 三邊都沒有留下半套資料，候選物件也沒被判重擋住，下一班會自動重抓。
全域錯誤告警正確響了。

**學到什麼**：
1. **用臨時 workflow 試跑 Gemini 會吃正式線的額度**，這是「只讀試跑」手法唯一會傷到線上的地方。
   要測就控制次數、挑非整點，或改用別的模型（配額每個模型分開算）。
2. **這條 20/天 的天花板本來就存在，只是沒人知道**。線 A 6 班 + 線 C 6 班 + LINE 三個指令，
   本來就貼著上限在跑。哪天使用者多打幾次「客戶」「行事曆」，廣告線就會在 `產文案` 靜靜失敗。
   要根治只有兩條路：**分散到不同模型**，或去 Google Cloud 開帳單轉付費方案。

---

## 2026-08-03 ─ AI 回傳壞掉時硬發，粉專出現整包 JSON

**症狀**：粉專出現兩篇貼文，內文是一整包 `{"粉專主體":"…","社團主體":"…"}` 原始 JSON，
連 `\n` 都是字面上的反斜線 n。08-03 那篇是使用者自己看到的；
**07-30 那篇（左營榮總路 898 萬）在牆上掛了 4 天沒人發現**。

**成因**：Gemini 吐完合法 JSON 後又進入重複迴圈，再接一長串
`詳情看留言"} 留言索取"} 言"}…`，整段就 `JSON.parse` 不動。
而 `解析文案` 節點舊版的保底寫法是

```js
catch (e) { parsed = { 粉專主體: aiText.trim(), 社團主體: aiText.trim() }; }
```

設計意圖是「壞掉也要發」，實際效果是「壞掉就發垃圾」。
整條線 0 錯誤、FB 回 200、守門員放行（那串垃圾裡沒有數字，數字檢查看不到它），所有告警全部沉默。

**已修（三支 workflow，已部署）**

| 改在哪 | 改成什麼 |
|---|---|
| `解析文案+footer`（scan-publish／repost）| 三層：正常 parse → 正則單獨救回兩個欄位 → 都失敗就**留空**，絕不回填原始字串。另外輸出 `_copyRescued`、`_主體長度` |
| `數字守門員`（scan-publish／repost）| 加第 (3) 項「出貨前形狀檢查」：文案含 `"粉專主體"`／`"社團主體"`／字面 `\n`／``` ``` ``` 就擋；主體 <60 字或社團 <15 字也擋。走現成的「Notion 建草稿列 → LINE 守門員告警 → 換下一件」路徑，**不會炸掉整班** |
| `解析文案`（yc-fb-publish，沒有守門員）| 同樣的救援，救不回就直接 `throw`，交給錯誤告警流程 |
| `ad_watchdog.py` | 加判斷三：最近 30 筆「已發布」的文案含 JSON 殘骸就告警 |

四個案例都跑過（正常／重複垃圾（真實事故那份）／完全不是 JSON／被截斷），行為符合預期。

**已善後**：兩篇 FB 貼文用 Graph API 改回正確文案（貼文網址、觸及、按讚都保留）；
Notion 那兩列的 `粉專文案`／`社團文案` 也一起洗乾淨。

---

## 2026-08-01 ─ Railway `SAVE_ON_SUCCESS=none` 弄死所有 Wait 節點

### 🔴 結論先寫：`EXECUTIONS_DATA_SAVE_ON_SUCCESS` 一律保持 `all`

**改成 `none` 會讓所有含 Wait 節點的流程整條死掉，而且死得無聲無息。**

**它是怎麼被設上去的**：2026-08-01 上午 Postgres 磁碟（當時 500MB）被執行紀錄塞滿，
n8n 整個靜悄悄停擺 4 小時（09:24～13:35，Web UI／API 照回 200，是使用者發現粉專沒發文才查出來）。
當天的治本方案一口氣做了四件事——volume 擴到 5GB、`EXECUTIONS_DATA_PRUNE=true`、`MAX_AGE=336`，
**外加 `SAVE_ON_SUCCESS=none`**，18:35 部署到 Primary + Worker。
前三件就已經解決問題，第四件是多做的，而它在 5 小時後（19:00 那班）弄死了所有 Wait 節點。

**為什麼會死**：
- 掃描發文線的「煞車 10 分鐘」是 Wait 節點，靠**把執行凍結存進資料庫、10 分鐘後撈出來叫醒**
- 紀錄不存 → 撈不到 → 永遠叫不醒 → 每班都正常建 Notion 待發列、正常發 LINE 預告，然後蒸發
- 08-01 19:00～08-02 15:00 共 **5 檔卡在「待發」沒發出去**，n8n 介面上完全看不到錯誤（失敗現場被自己抹掉）

**修復**：2026-08-02 16:00 兩台改回 `all` 並重新部署，執行紀錄恢復（id 4173 → 4320，中間約 147 筆
是跑過但被丟掉的）。17:00 那班驗收通過：執行 10 分 28 秒走完，AA1069194 已發布、粉專連結有值。
卡住的 5 筆已在 Notion 封存（判重只看案件編號在不在，封存後自動回到候選池重排隊）。

**空間問題早就根治了，不要再拿這招止血**：volume 5GB、`PRUNE=true` + `MAX_AGE=336`（14 天自動清）。
2026-08-02 實測只用 914MB，不到兩成。真的又不夠時，正確做法是**調降 `MAX_AGE`**（336 → 168）。

> **診斷心法**：n8n 執行紀錄整個空白、但 Notion／LINE 卻有新東西冒出來 = 紀錄層壞了，不是流程壞了。
> 交叉驗證用桌面 grab.py 的心跳（每 10 分鐘一次，n8n 收不到就是紀錄層有問題）。

### 為什麼三道告警全部沉默（別再把監控加在 n8n 裡面）

| 監控 | 看什麼 | 那天為什麼沒響 |
|---|---|---|
| 系統錯誤 LINE 告警 | n8n errorTrigger | 根本沒有錯誤，執行是「等待中蒸發」，n8n 認為沒事發生 |
| 靜默失敗巡邏 | 讀 `/api/v1/executions` | **讀的就是壞掉的那個東西**，清單空的 → 判定「這半小時沒新執行，一切正常」 |
| KEIS 心跳檢查 | 桌面 grab.py 活著沒 | 看的是另一個系統，grab.py 一直好好的 |

共同點：三道都在看「機器有沒有在動」，而這類事故正好是**把機器弄啞**，於是監控跟著啞。
上次事故後補的「靜默失敗巡邏」加在 n8n 裡面，等於在同一條船上多派一個瞭望員，船底破洞一起沉。

**結論：監控不准住在 n8n 裡，而且要看結果不看過程。** → 廣告發文看門狗（見 reference.md）。

### 8/1 那次修復的兩個補丁「都是壞的」——最該記住的一課

同一次修復加的兩道防護，2026-08-02 檢查發現**兩道都沒效**：

1. `SAVE_ON_SUCCESS=none`——不但沒必要（前三件事已經解決空間問題），還**製造了新的無聲故障**
2. grab.py 繞過 n8n 直推 LINE——**token 只加進 repo 的 `.env`，沒加進桌面那份**，
   而真正在跑的是桌面版，所以那條防線從頭到尾只會靜靜寫一行 log

共同病灶：**改完沒有在「真正在跑的環境」實地驗一次**。

---

## 2026-08-01 ─ 官網物件編號會撞號，差點連錯別人家的物件

**症狀**：`LA0067094`（大樹區土地）比中 `YE0067094`（新北淡水，別人家的物件）。

**成因**：各品牌各自發號，數字部分會撞號。原本的比對邏輯只看「數字部分完全一致」。

**修法（v5 撞號防線）**：編號對上只是必要條件，還要**行政區對得上 + 至少一項佐證**
（總價／建物坪／權狀坪／土地案用地坪或地段名），對不上就記「⚠️編號撞號擋下」跳過，不猜。
50 筆現有正確配對實測過：行政區/總價 100% 通過，不會誤擋；
坪數不能單獨當硬門檻（土地案沒建物坪會被誤傷 4 筆），只當佐證之一。

掃描發文線、重發輪替線兩條都套用（重發線原本是更舊的「路名+總價」版，路名對土地案是空的）；
下架偵測線靠 KEIS 自己的廣告追蹤+存活檢查，不比對物件身份，沒有撞號風險。

歷史遺留的 4 筆壞連結（1 筆撞號連錯、3 筆是舊版直接把 KEIS 內部 houseol.com.tw 連結寫進去）
已於 2026-08-01 全部修復並驗證（commit 615a29e / f576107）。

---

## 2026-07-29 ─ 誤發 `show_in_web=false` 的案子

### 🔴 紅線：KEIS `show_in_web=false` 的案子絕對不發（使用者定案）

那代表屋主/門市沒同意網路露出。

**事故**：07-29 早上誤發過一件 YG0092410（永義房屋楠梓益群藍田，只有 1 張照片、沒官網連結）。
貼文已用粉專權杖刪除、Notion 那列已 archive（Notion 裡可還原）。

**規模**：全體 7171 筆在售案裡有 **105 筆** `show_in_web=false`，其中 **83 筆**符合舊的候選條件
（≥800 萬＋有照片）——紅線加進去之前，這 83 筆隨時可能被抽中。

**修法**：`篩選候選` 加 `x.show_in_web === true`；`API 體檢` 的必要欄位也加了 `show_in_web`——
這個欄位哪天被 KEIS 砍掉會先推 LINE 告警，不會靜靜變成「全部案子都不發」。

**已全面稽核**：Notion 狀態=已發布的 39 則對 7171 筆在售案，**0 則踩紅線**
（37 則 `show_in_web=true`，2 則是 v2 舊資料 YC1868705 / YE0095535，KEIS 裡本來就沒有）。

⚠️ **archive 是必要的**：判重只看「案件編號在不在 Notion」不看狀態，留著那列的話
KEIS 以後把 `show_in_web` 改回 true，這件也永遠不會再被抓到。
現在紅線擋在 `篩選候選`（更上游、不建 Notion 列），不會再產生這種殘列。

---

## 2026-07-28 ─ 三個同源問題：失敗被吞掉，全部靜悄悄

### (1) 下架刪不掉貼文而且沒人知道

花賞（YC1868650）07-27 22:00 被正確判定下架，Notion 也標了「下架」，
但**粉專那篇沒被刪、也沒推 LINE**。兩個原因：

1. `備妥刪文` 的正則只認 `facebook.com/{粉專ID}_{貼文ID}`，而 v3 發文線寫進 Notion 的是
   `facebook.com/{粉專ID}/posts/{貼文ID}` → **在架上 28 件有 25 件解析失敗**，
   等於整條下架線根本刪不掉任何一篇 v3 發的文。已改成三種格式都吃
   （`_` 格式／`/posts/` 格式／數字型 `story_fbid=…&id=…`），用線上程式碼實跑驗過
2. 更糟的是 `判定刪除結果` 把「解析不出貼文 ID」當成「本來就沒貼文要刪 → 成功」，
   所以 `fbDeleteFailed=false` → 不進刪文失敗通知。已改成：**有連結卻解析不出＝刪文失敗**，
   照推 LINE 叫人手動刪；同時仍把 Notion 標下架（否則每天重試都會失敗、天天洗同一則通知）

`pfbid` 開頭的舊連結是不透明字串，本來就還原不出貼文 ID，只能走人工路線。
線 C `挑一件重發` 的解析器本來就 `/posts/` 優先寫對了，**沒有**線 B 這個 bug。

### (2) 🔴 為什麼同類問題一再發生 —— 靜默失敗巡邏的由來

07-27 加的「系統錯誤 LINE 告警」**只在執行狀態＝error 時才響**。
但這一連串問題全都是節點設了容錯（`onError=continueRegularOutput` 或 `neverError`），
失敗被吞掉、執行狀態仍是 success → 告警一次都不會響。
啟用中 16 支 workflow、191 個節點裡有 **41 個**是這種。
＝之前修的都是「那一次的症狀」，沒有任何東西在看「這一類」。

新增 **`靜默失敗巡邏.json`（每 30 分鐘）**：掃最近的成功執行，找出節點輸出裡帶 error 的，推 LINE。
用 `workflowStaticData.lastExecId` 記進度不重複推；第一次跑只設基準線不翻歷史。
上線前對 60 筆歷史執行離線試掃，零誤報，並用一次性複本實跑驗過訊息內容。

**它當場多抓到一個沒人知道的問題**：線 A `LINE 守門員告警` 讀 `$json._guardMsg`，
但上游是 `Notion 建擋下列`（Notion 回應）根本沒這欄位 → 推空字串 → LINE 回 400
→ **守門員擋下物件的告警從來沒送達過**。已改讀 `$('數字守門員')`。

### (3) LINE 通知被 500 靜靜吃掉

07-28 17:00 那班廣告其實發成功了（貼文 `…122112148101336834`、KEIS adcase `335145`、
Notion 也寫回了），但最後的 `LINE 發布通知` 被 LINE 回 500 → 使用者沒收到通知。
節點是 `onError=continueRegularOutput`，執行狀態仍是 success，**連全域錯誤告警都不會響**。

已把**全部啟用中 workflow 的 LINE push 節點加上重試**（3 次、間隔 5 秒，共 9 支）。
重打 LINE 最壞只是多一則訊息，跟 FB 發文／KEIS 建檔那種「重打會產生重複資料」不同。

順手修掉 `取 KEIS 廣告ID` 讀舊欄位名 `ycResolveNote`（實際是 `ycNote`），
以前每次成功都會印成「已同步（連結用備援：undefined）」。

### 附帶驗證：刪文能力已端對端實測

發一篇 `published:false` 草稿 → 用下架線同一支 DELETE 刪掉 → 回 `{"success":true}` → 再查已不存在。
草稿不進動態牆，要再驗可以照這個方法，不用拿真貼文冒險。

---

## 2026-07-27 ─ FB 回 500 不等於沒發出去

### 🔴 最重要的一課（AA1070116）

07-27 15:00 那班 execution 2235，前面 31 個節點全過（文案、守門員、10 分鐘煞車、
10 張照片上傳都成功），最後 FB Graph API 回 500 `code 1 "Please reduce the amount of data..."`。
那句是 FB 的萬用錯誤，**不是真的資料太多**（同日 09/11/13 三班同樣 10 張照片都成功）。
當時沒有任何告警，使用者是自己發現廣告沒發。

**而那班的貼文其實建立成功了**（`is_published:true`、內容正確、15:10:28 上線），只是 API 回了 500。

所以「失敗就重發」會產生重複貼文、「失敗就撤列等下一班」會白白損失一班，**兩個都不對——
要先向 FB 反查「到底發出去了沒」**。查法（現有權杖就能做）：

- ❌ `GET /{page}/feed` → 400，權杖缺 `pages_read_engagement`
- ✅ `GET /{media_fbid}?fields=page_story_id` → **通**。發文流程是先上傳照片再組成貼文，
  照片有 `page_story_id` 就代表已經被組成一篇了；再 `GET /{page_story_id}?fields=is_published` 確認

**線 A 補救分支**：`FB 發貼文` 失敗 → `查照片是否已成篇` → `判斷補救方式` →
- ① 其實成功 → `確認貼文存在` → `真的已發布?` → `補回貼文結果` → **接回 `取貼文結果`**，
  後面的 Notion 標已發布／KEIS 廣告追蹤／LINE 通知全部照常跑完
- ② 確定沒發出去 → `重發一次`（此時重發零重複風險）→ 再失敗才撤列等下一班
- ③ 問不到 FB（照片讀取失敗／純文字貼文）→ 保守不重發，撤列等下一班

⚠️ **三線同步很重要**：那次中斷害 KEIS 廣告追蹤沒登記到，已人工補登（adcase `333341`）。
Notion／FB／KEIS 任一邊漏掉，之後線 B 下架時就刪不乾淨。

### 同批加的兩件事

- **`系統錯誤-LINE-告警.json`**（Error Trigger → 組訊息 → LINE Push），
  已掛成全部啟用中 workflow 的 `errorWorkflow`。訊息帶流程名／卡住的節點／錯誤內容／執行連結。
  ⚠️ **這支一定要保持 active**（實測：inactive 時完全不觸發）
- **線 A `FB 發貼文` 開錯誤輸出**：發文失敗 → `Notion 撤銷待發列`（PATCH `archived:true`）→ LINE。
  為什麼要撤列：判重只看案件編號不看狀態，而待發列是發文前就建的，
  不撤列的話那個物件會被永遠跳過。撤掉後下一班會自動重抓同一件

---

## 2026-07-25 ─ 外部服務抖一下就整條線陣亡

07-24 掉了兩班：13:00 `KEIS 下載照片zip` ECONNABORTED、19:00 `查 Notion 是否已建` HTTP 522，
都是對方伺服器抖一下。

**修法**：線 A/線 C 的 KEIS、Notion、Gemini、FB 上傳節點全部設 `retryOnFail=3 次 / 間隔 5 秒`。

⚠️ **`FB 發貼文`／`Notion 建待發列`／`KEIS 新增廣告`／`刪舊貼文` 刻意不加重試**——
重打會產生重複貼文/重複資料，寧可失敗也不要重複。
（2026-07-27 一度給 `FB 發貼文` 加了重試，發現違反這條又拿掉。）

---

## 2026-07-23 ─ KEIS 突然砍 API 欄位，線 A 連續空轉兩班

**事故**：KEIS 當天下午把兩支 API 砍瘦——列表 40→18 欄、詳情 149→61 欄，
**照片網址 `images` 整組消失**，連帶掉了 `official_url`／`age`／`school_info`／`layout`／`floor_info`
（`seller_*` 屋主個資也一起被砍，研判是刻意收緊）。

**修法（六項，都已上線並試跑驗過）**

| 改動 | 內容 |
|---|---|
| 照片改走 zip | `GET /api/v1/property-management/{id}/download-images` → 回 zip（實測 17 張 800KB）→ n8n `解壓照片`（Compression）→ `照片分筆`（一筆一張、最多 10 張）→ `FB 上傳照片` 改 **multipart 傳檔案**（原本是給網址）。用現有 KEIS 自動登入 credential |
| official_url | 詳情沒了，改從**列表**帶進 `白名單清洗` |
| 格局／樓層 | 自己組：房廳衛只寫有數字的、土地類留白；樓層起訖不同寫 `1-4/4`、地下室寫 `B1`、缺總樓層就留白。100 筆實測 vs KEIS 原字串：格局 91%、樓層 99% 一致，不一致的全是土地/廠房那類（我們留白，不亂寫）|
| 屋齡／學區 | 新節點 **`永慶官網補資料`**：沿用線 D 反查邏輯拿到官網頁，解析屋齡、學區，**並用官網公開版本交叉驗證格局樓層，不一致就留白＋記註記**。查不到就整個留白（不影響發文）|
| **數字守門員** | 新節點：文案裡每個數字都要在官方事實裡找得到，對不上就**不建 Notion、不發文**，改推 LINE。另擋「官方沒這筆資料就不准提」，防 AI 腦補。⚠️ 比對前先把網址拿掉 |
| **API 體檢** | 撈完列表先檢查 `contract_no/case_price/image_count/official_url/is_active/houseol_created_at/show_in_web` 還在不在，缺了就**停這一輪 + LINE 告警** |

**Gemini prompt 也收緊了**：粉專文案只准四塊（標題／規格區／✨亮點 3-5 條／footer），
**禁止自由描述段落**。原因：實測時它會自己從「全新未住」腦補出「屋齡 1 年」「前後陽台」。

**線 C 重發輪替同步改完**（吃同一套詳情 API 和照片來源），節點數 23。

**衍生（2026-07-26）**：
- **候補接棒**：AG1927883 被守門員擋、沒建 Notion，導致它連續霸榜候選第一名、每班空轉。
  改成 `取候補 3 件` + `逐件嘗試` 迴圈，被擋的建 Notion 草稿列（備註寫原因）。詳見 PR #123
- **賣點預過濾**：賣點常寫「近學區」但官網沒登記，硬留著會被守門員判腦補整篇擋掉。
  從源頭砍句子比事後擋整篇好，標題也一併解決（標題是賣點延伸出來的）。詳見 PR #124

---

## 2026-07-22 ─ v3 上線首日修掉的三個 bug

線 A 手動跑 → KEIS 撈案、Gemini 產文案、Notion 建列、LINE 預告、10 分鐘後自動發粉專多圖文全部正常，
第一篇真實廣告上線（YG0158419）。過程修掉：

1. 面議價 9999 萬會被當真實價（已加價格過濾）
2. 完成通知抓錯欄位
3. FB permalink 格式

Notion「狀態」已加 `待發` / `取消` 兩個選項（**select 選項不存在會讓 Notion query 直接回 400**）。

---

## 2026-07-16 ─ 測 webhook 的中文編碼坑

在 Windows 終端機用 `curl` 直接帶中文測 webhook 會被 cp950 弄壞字元造成**假故障**。
測中文 payload 一律用 python httpx。

---

## 2026-07-29 ─ 搶單「補回」誤記值班分派名單

`reconcile_applications()` 原本以為 `only_my_applications=true` 回的是「我申請過的」，
其實是「掛在這帳號名下的全部名單」，裡面混著 `status=DutyAssigned`、`app_time=null` 的名單
（KEIS 直接給到帳號的，沒經過公開池）。舊版沒看 status 就把它們當「漏記的搶單」
補進 grabbed.csv／Notion／LINE。

**修法**：加 `is_our_application()`：`status=="Applied"` **且**有 `app_time` 才算搶到；
被擋的記 `ℹ 略過 N 筆非搶單名單`。順手：補回的 LINE 訊息改成真查配額（原本寫死 0）
＋標明「不是機器人搶的」＋非手機標 ☎️市話；再加欄位體檢，`status` 整批消失會推 LINE 告警。

**本機帳已清**：grabbed.csv 168→123 筆（移除 45 筆非手機，備份 `grabbed.csv.bak-20260729-100357`、
移除清單 `removed-nonmobile-20260729-100357.csv`），daily_summary 07-28 改回「打中 6／補回 0」。

**Notion 刻意不動**：「戰果」只查當天，歷史那 38 筆不會出現；而且那些是真客戶，
「待聯絡提醒」正靠 `搶到時間` 追他們的 7 天期限，刪掉會害人漏打電話。

**查證方法留著**（以後再遇到「這筆哪來的」照這個做）：用**沒持有該筆的另外兩個帳號**
去 `query_deep()` 看得到看不到——別人搶走的會顯示 `CoolingDown`，79300 三個帳號全看不到＝沒進過池子。
用自己帳號查是無效的（自己名下的會從自己視角消失）。

⚠️ **未解**：`DutyAssigned` 在 KEIS 內部的正式名稱和觸發者不明。已知的只有：沒 app_time、
不吃搶單配額（`my_7day_count` 不計它）、三帳號都看不到它在池子裡。
要確認得登入 KEIS 網頁看 79300 掛在哪個分頁。

---

## 2026-07-28 ─ 搶單名單在 Notion 手動刪會被補回來

使用者刪掉的 13 筆舊名單被 08:34 的全表對帳全數補回，早上 LINE 因此比使用者印象中多。

**原因**：`audit_notion` 拿 `grabbed.csv` 當唯一真相，只認 summary_id 在不在，
刪掉＝它眼中的缺漏 → 補回來（平常回查 7 天，跑全表對帳則不限）。

**正確做法**：要讓一筆退場一律改「聯絡狀態」（無效空號／已聯絡…），
資料留著但不進提醒名單，對帳也不會當缺漏。

---

## 2026-07-30 ─ GitHub 帳號被停權（自我檢討）

**可能的觸發原因**：那天兩小時內開了 5 個 PR、每個開完幾秒就自己合併＋刪分支，
加上 commit 都帶 `Co-Authored-By: Claude`、分支都叫 `claude/...`，很像 bot 在刷。

**之後改成：一批工作一個 PR、不要開完立刻合、不必每次刪分支。**

現況與復原步驟見 `STATUS.md`。
