# STATUS — 現在的狀態與接下來要做的事

> **這份只放「現況」和「待辦」。完成的項目直接刪掉，不留歷史。**
> 查表（入口/credential/Notion 欄位/指令/工具清單）→ `reference.md`
> 事故經過與血淚史 → `incidents.md`
> 線上真正在跑什麼 → `n8n-live.md`（n8n_sync.py 產的，**兩邊打架信這份**）
> 外部 AI 工具情報（評估過的／已排除的，不用重評）→ `ai-toolbox.md`

最後更新：2026-08-09 晚上 ｜ 使用者：薛力瑜（永慶不動產 博愛凱璿加盟店）

---

## ✅ GitHub 帳號已恢復（2026-08-06）

- **待辦**：`桌面\asce-備份-20260730.bundle` 離線備份可以刪了（main 已跟 GitHub 對齊，08-07 確認還在）
- `qingan3-calc` 讀取障礙已解除，但**沒有本機原始檔的事實沒變**，要救得先找到原始檔（見第 4 節）

---

## 🔴 三條永久紅線（動之前先看 `incidents.md`）

1. **Railway `EXECUTIONS_DATA_SAVE_ON_SUCCESS` 一律保持 `all`**
   改 `none` 會讓所有含 Wait 節點的流程無聲死掉（煞車 10 分鐘就是 Wait）。
   空間不夠的正確解法是調降 `MAX_AGE`，不是關掉紀錄。
2. **AI 回傳壞掉時寧可不發，絕不硬發**
   保底邏輯只要「讓流程繼續」而不管「送出去的東西對不對」，就是在把故障洗成看起來成功的樣子。
   對外送出的東西一律要有形狀檢查。
3. **KEIS `show_in_web=false` 的案子絕對不發**（屋主/門市沒同意網路露出）。擋在 `篩選候選` 最上游。

**另外兩條通則**：監控不准住在 n8n 裡（見廣告發文看門狗）；改完一定要在「真正在跑的環境」實地驗一次。

---

## 系統現況

| 線 | 狀態 |
|---|---|
| 線 A 掃描發文（09/11/13/15/17/19）／線 B 下架偵測（09:30、22:00）／線 D KEIS 同步 | 🟢 active |
| 線 C 重發輪替（09:30～19:30 每 2 小時）| 🟢 active；**FB 發/刪那段還沒真的跑過** |
| 煞車（停）／ LINE 指令分流器 ／ 靜默失敗巡邏 | 🟢 active |
| 系統錯誤 LINE 告警 | 🟢 active（**必須保持 active**，否則不轉發）|
| 廣告發文看門狗（桌面，n8n 外，每小時）／公買搶單／自動簽到／買方配案排程 | 🟢 上線中 |

---

## ▶ 下次開工從這裡接

### ✅ 08-09 Postgres 撐爆停機，已完全修完（不用再看）

volume 100% → n8n 全停 ~1.5 小時，資料沒掉，22 支 workflow 全部復原。完整經過見 `incidents.md`。
根因（`靜默失敗巡邏` 每 30 分鐘複製 50MB 進自己的執行紀錄）已修：該 workflow 的
`saveDataSuccessExecution` 設 `none`（全域那條紅線沒動）。磁碟告警已上線且實測過
（`N8N_PG_DSN` 已填進兩份 `.env`，超過 70% 會直推 LINE）。**密碼輪換使用者已確認不換**。

⚠️ 新紅線：**清 n8n 執行紀錄一律 TRUNCATE，絕不用 API 批次 DELETE**——這次停機就是這樣造成的。

### 0.1 ★ 守門員誤擋：第 10 個 bug 已修（08-10），**觀察接下來幾班**

3 週實測誤擋率 **11:1**（94 篇發布／11 筆誤擋重跑／只有 1 筆擋對）。全部同一個病：AI 改寫數字寫法
（全形、元→萬、7-11、民國年、千分位、**中文數字**），守門員只逐字比對。08-10 除了補中文數字換算，
已在 `Gemini 產文案` 加「數字照原文抄、不得換算」堵源頭。經過見 `incidents.md`。
AG1918052／AG1918071 已改名重排，等排程回鍋。

⚠️ **改守門員只能「補真出處」，不能「放寬檢查」**。AG1869334「步行 8 分鐘直達 O4」是 AI 自己編的、
**擋對了**，任何讓它過關的改法都是把洞打開。收到「交叉審核擋下」的 LINE 仍要去看理由站不站得住腳。

**待決定（都還沒做）**：① Google Cloud 開帳單（月費不到 100，零改動，買掉免費版 503／配額的不確定性）
② 煞車 10 分鐘實測 3 週沒人用過「停」，考慮縮短 ③ 文案改純套版可一次解掉整類問題，但要先看文案。

### 0.2 Notion 擋下列漏寫理由，08-07 晚上已修上線

`Notion 建擋下列` 原本漏帶 `_guardReview`（交叉審核）和 `_guardJunk`（格式壞掉），
被這兩類擋下的案子理由永遠空白。已補。**理由欄是判斷誤擋的唯一依據，再空白就是又漏了哪一類。**

⚠️ **廠房／土地「格局空白」是正常的**（開放空間沒房間），不要為了這個放寬守門員。

### 0.5 🔴 Gemini 免費配額每天只有 20 次（每個模型），系統本來就貼著上限在跑

線 A 6 班 + 線 C 6 班 + LINE 三個指令共用 `flash-lite`，已接近 20，**使用者多打幾次「客戶」
「行事曆」就會讓廣告線在 `產文案` 靜靜失敗**。（交叉審核走 `flash`，是另一桶 20 次，不相干。）
根治：分散到不同模型，或開 Google Cloud 帳單轉付費（月費不到一杯飲料）。細節見 `reference.md`。

### 0.8 KEIS 被砍的欄位（`images`／`age`／`school_info`…）2026-08-04 都回來了，**先別改回去**

現在的繞道做法比較耐打，KEIS 砍過一次就會砍第二次。詳見 `reference.md`。

### 1. AG1911938 鳳山鳳松路透天：過兩天再看

KEIS 資料完整、編號有效（1580 萬／空屋），但**官網真的還沒有**（編號差 1 號的 AG1911937 有
→ 不是搜尋壞掉）。只能看「連續幾天都查無 = 大概真的不上網」。
⚠️ 坪數**不是填錯，別去問店家**：有加蓋，單價 110 萬/坪 是拿登記坪當分母才顯得離譜。

### 2. 兩段還沒被真實事件走完的流程（都各自驗過了，等自然發生）

- **FB 補救分支**：等 FB 真的打嗝。收到「❌ 廣告沒發出去」就看該班 `判斷補救方式` 的 `mode`
- **線 C 的 FB 發新文＋刪舊文**：程式路徑已就緒但**還沒被真實事件走完**。2026-08-05 起線 B
  看到 KEIS 掛「降價」標籤就自動勾 `要重發`，線 C 沿用舊文案只換價＋插一行「🔻 降價 X 萬」，
  發完回寫 Notion 總價與 KEIS `adcase_price`／FB 連結。**首件 EG0506900（1298→1180）等點頭才勾**

### 3. 買方配案（機制細節見 `reference.md`）

- **2026-08-09 修**：物件卡片分享連結消失兩天（08-08 20:14 起），是開新分頁後讀 `.url`
  讀太早、抓到空字串，已改輪詢等真網址。今天已跑完的結果檔沒有連結，**要等下一班排程
  重跑才會補回**。順便發現排程用 `pythonw.exe` 沒接 stdout/stderr，失敗了 log 也看不到
  ——這個洞還沒補。經過見 `incidents.md`
- **2026-08-08 修**：類型（大樓/透天）跟用途（店面/住宅）拆開比對改用 AND，不再混成
  同一包 OR——修前透店客需會混進純住宅大樓案。經過見 `incidents.md`
- **2026-08-08 上線**：看板改成同客戶多客需歸類成一張卡（不再重複列名字）；
  房地改版拿掉 CSS class 害選擇器讀成 0 筆、當天排程整批空跑一天，已修＋補跑，
  經過見 `incidents.md` 2026-08-08「房地改版拿掉 CSS class」那則
- **⏰ 2026-08-11 後刪掉 `C:\Users\user\_待刪_20260804_買方配案舊登入態\`（4.72 GB）**
  兩份停用的 Chrome 登入態，08-04 搬出來但沒刪、觀察一週。刪前看 `daily_run.log` 有沒有正常跑
  （08-07 09:52 有跑）。徵兆是 LINE「🔑 房地要重新登入」→ 掃一次 QR Code 即可，不用搬回來。
- **待補**：車位型態（平面式/機械式）還沒對應，目前只做到「有/無車位」
- 手機看板 https://yc-tools.pages.dev/buyer-match/ （排程跑完自動部署）

### 4. 公開網頁在 Cloudflare Pages（2026-08-05 上線，發布鏈路見 `reference.md`）

- 三合一計算機 https://yc-tools.pages.dev/yc-calc/
- 行情看板 https://yc-tools.pages.dev/kh-market/
- 買方配案看板（內部用，`robots.txt` 擋收錄）https://yc-tools.pages.dev/buyer-match/

⚠️ **`qingan3-calc` 帳號恢復了但還是 404**：GitHub 讀取被擋那個技術性障礙已解除，
問題回到本質——**沒有本機原始檔**，要救得先找到原始檔才動得了。

---

## 其他待辦

- **Notion「來源連結」欄已刪（2026-08-05）**：存的是會過期的 houseol 網址（刪除前 72 筆全部 404）。
  引用全改指 `永慶官網連結`（線 B 二次確認／`publish.py`／yc-ad skill）。**不要再加回來**，理由見 `reference.md`
- **Notion v2 舊資料**：YC1868705 還在、不在 KEIS，線 B 掃到略過；它的 `永慶官網連結` 已補上
- **公買搶單備註漏寫已修**（08-08，經過見 `incidents.md`）：補寫路徑會清空備註，已補
  CSV 欄位＋每天跟 KEIS 對帳自動修。三帳號都會查，`--audit-remarks` 可手動跑
- **未來候補**：多開一個 IG 帳號專發廣告。使用者要先自己把新 IG 轉商業帳號、綁粉專＋開權限，
  之後才接 n8n（IG 是兩段式 container→publish）。目前卡在帳號還沒建，不用主動催
