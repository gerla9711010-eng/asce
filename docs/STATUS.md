# STATUS — 現在的狀態與接下來要做的事

> **這份只放「現況」和「待辦」。完成的項目直接刪掉，不留歷史。**
> 查表（入口/credential/Notion 欄位/指令/工具清單）→ `reference.md`
> 事故經過與血淚史 → `incidents.md`
> 線上真正在跑什麼 → `n8n-live.md`（n8n_sync.py 產的，**兩邊打架信這份**）
> 外部 AI 工具情報（評估過的／已排除的，不用重評）→ `ai-toolbox.md`

最後更新：2026-08-17 ｜ 使用者：薛力瑜（永慶不動產 博愛凱璿加盟店）

⚠️ n8n_sync 連不到 n8n（timeout，cloudflared 通道問題）：`yc-v3-scan-publish.json` 還有 1 支沒同步，下次先重跑 `--check`

---

## 🟢 2026-08-14：FB 廣告工作鏈（線 A/B/C）重新開啟，全面拔掉主動 LINE 推播

08-12/13 的「全停」對線 A/B/C／路由器／系統錯誤告警**已解除**，其餘項目（見下方系統現況表）
維持停用，沒動。**LINE 額度吃緊**，原本 11 個主動 push 的地方全部拔掉，改寫進新的 Notion DB
「系統日誌」（`0df73bdfe72b4451bb841ebc151aecc9`），LINE 打關鍵字 **「工作回報」** 才查、才回
（reply 不吃 push 額度）。已實測成功：Notion 查詢＋組訊息都正常，當天真的有發布一篇廣告
（AG…高雄小天母｜河堤綠意森活）。架構細節見 `reference.md`「現有架構」與「推播 → 系統日誌」。

- 線 A 的「10 分鐘煞車預告」推播也拔了（使用者確認不需要）；`煞車 10 分鐘` Wait 節點保留，
  「停」指令照常能攔（走 reply，本來就不吃額度）
- 系統錯誤 LINE 告警改寫系統日誌、繼續保持 active
- 線 A「產文案」已接上 `scripts/codex-copy`，開機捷徑已放回 `shell:startup`；
  cloudflared 隧道仍可能斷，8/12~8/13 斷過害 5 班空跑，穩定性待觀察，見 `incidents.md`
- 「工作回報」原本每天問都整批重報舊資料的 bug 已修（改用游標記上次查詢時間，見 `reference.md`）
- **LINE 額度精簡第二輪（08-14）**：搶單通知／簽到通知也改寫進系統日誌，不再主動 push
  （這兩支是「例行成功也推播」的，量小但持續在燒）。心跳告警／待聯絡提醒／情資週報
  這種「只在異常才推」的還沒動，量小，之後有空再一起改

---

## 🔴 三條永久紅線（動之前先看 `incidents.md`）

1. **Railway `EXECUTIONS_DATA_SAVE_ON_SUCCESS` 一律保持 `all`**
   改 `none` 會讓所有含 Wait 節點的流程無聲死掉（煞車 10 分鐘就是 Wait）。
   空間不夠的正確解法是調降 `MAX_AGE`，不是關掉紀錄。
   清執行紀錄一律用 **TRUNCATE**，絕不用 API 批次 DELETE（08-09 停機經過見 `incidents.md`）。
2. **AI 回傳壞掉時寧可不發，絕不硬發**
   保底邏輯只要「讓流程繼續」而不管「送出去的東西對不對」，就是在把故障洗成看起來成功的樣子。
   對外送出的東西一律要有形狀檢查。
3. **KEIS `show_in_web=false` 的案子絕對不發**（屋主/門市沒同意網路露出）。擋在 `篩選候選` 最上游。

**另外兩條通則**：監控不准住在 n8n 裡（見廣告發文看門狗）；改完一定要在「真正在跑的環境」實地驗一次。

---

## 系統現況（2026-08-14）

| 線 | 狀態 |
|---|---|
| 線 A 掃描發文／線 B 下架偵測／線 C 重發輪替（線 C 的 FB 發/刪**還沒真的跑過**） | 🟢 已開（08-14）|
| 線 B 下架偵測（`yc-v3-removal.json`）**08-12 09:30 後排程停跳**（active=true 但 08-12 22:00～08-14 09:30 共 4 班沒執行紀錄，典型「active 沒重觸發」）。08-14 已 deactivate+activate 重置，**下一班 22:00 要確認有沒有真的跑** | ⚠️ 待驗證 |
| 系統錯誤 LINE 告警（改寫系統日誌）／工作回報查詢（新） | 🟢 已開（08-14）|
| 靜默失敗巡邏／KEIS 待聯絡提醒／KEIS 心跳檢查 | ⚪ 已停（沒動，08-12/13 全停留下的）|
| 廣告發文看門狗／公買搶單／自動簽到／買方配案排程／市場週報／行情看板更新 | ⚪ 已停（沒動）|
| KEIS 搶單 LINE 通知／自動簽到 LINE 通知 | ⚪ 已停（沒動，這兩支還是主動推播）|
| 煞車（停）／LINE 指令分流器／圖片分流器／客戶建檔器／行事曆建立器／戰果查詢 | 🟢 active（LINE 觸發，沒時鐘）|
| 情資週報 | 🟢 active，只剩「情資」關鍵字回覆能用（推播分支 node 已停）|

---

## ▶ 下次開工從這裡接

### 0.1 守門員誤擋：第 10 個 bug 已修（08-10），**線已重開，觀察接下來幾班**

3 週實測誤擋率 11:1（94 篇發布／11 筆誤擋重跑／只有 1 筆擋對），已在 `Gemini 產文案` 堵源頭
（PR #186／#187）。經過見 `incidents.md`。⚠️ 改守門員只能「補真出處」不能「放寬檢查」，
收到「交叉審核擋下」仍要去看理由站不站得住腳。

### 0.15 Codex 文案服務**已接線**（`scripts/codex-copy/`，取代 Gemini 免費配額）

⚠️ **不穩**：cloudflared 通道每次重開網址都會變，斷過好幾次害線 A 空跑（見 `incidents.md`）。
開機捷徑已放回 `shell:startup`，重開機會自動接通道回寫 n8n，但**通道本身斷掉沒有退路**，
斷線頻率還要觀察。

**已決定不做**：煞車縮短、文案純套版、Google Cloud 開帳單、交叉審核換 Codex（理由見 `reference.md`）。

⚠️ **廠房／土地「格局空白」是正常的**（開放空間沒房間），不要為了這個放寬守門員。

### 0.5 Gemini 免費配額每天 20 次（每個模型）——08-14 起壓力變小

線 A「產文案」已改走 Codex（見 0.15），**不再吃這桶配額**。現在共用 `flash-lite` 的只剩
線 C 6 班 + LINE 三個指令，比之前寬鬆很多。（交叉審核走 `flash`，是另一桶 20 次，不相干。）
細節見 `reference.md`。

### 0.8 KEIS 被砍的欄位（`images`／`age`／`school_info`…）已修復，**先別改回去**——見 `reference.md`

### 1. AG1911938 鳳山鳳松路透天：過兩天再看

KEIS 資料完整、編號有效（1580 萬／空屋），但**官網真的還沒有**（編號差 1 號的 AG1911937 有
→ 不是搜尋壞掉）。只能看「連續幾天都查無 = 大概真的不上網」。
⚠️ 坪數**不是填錯，別去問店家**：有加蓋，單價 110 萬/坪 是拿登記坪當分母才顯得離譜。

### 2. 兩段還沒被真實事件走完的流程（都各自驗過了，等自然發生）

- **FB 補救分支**：等 FB 真的打嗝。收到「❌ 廣告沒發出去」就看該班 `判斷補救方式` 的 `mode`
- **線 C 的 FB 發新文＋刪舊文**：程式路徑已就緒但**還沒被真實事件走完**。2026-08-05 起線 B
  看到 KEIS 掛「降價」標籤就自動勾 `要重發`，線 C 沿用舊文案只換價＋插一行「🔻 降價 X 萬」，
  發完回寫 Notion 總價與 KEIS `adcase_price`／FB 連結。**2026-08-14 使用者已同意，EG0506900
  （1298→1180）`要重發` 已手動勾上，等下一班線 C 跑**——跑完看有沒有走完整條路徑，沒有就記錄卡點

### 3. 買方配案：永慶連結抓取＋看板**08-17 已上線**（PR #205/#206/#207 已合併，第四支待開）

`python run_yc_links.py <群組>` 手動跑（改用 foundi 候選卡片抓連結，細節見
`scripts/buyer-match/yc_link.py`），跑完自動存檔／標新出現／產看板／部署到
`yc-tools.pages.dev/buyer-match/`。看板：按群組分頁籤切換、單筆/整批/複製全部
三種一鍵複製（標題＋開價＋連結）、每筆顯示永慶自己標的上架時間當客觀對照
（跟🆕徽章分開放，🆕只代表「系統這次才看到」不等於真的剛上架）。

去重記憶（`seen_store.py`）用房地子條件 DOM 自帶的穩定 id 索引，不是名稱字串，
子條件改名不會害記憶失效——見 `scripts/buyer-match/README.md`。

實測：全 A買+B買+C買（43 個子條件）跑完 0 連線錯誤、部署成功、線上驗證過。

⚠️ 已知小狀況：規模夠大偶有個別候選點擊逾時（容錯機制擋住，不影響其他資料）；
診斷用一次性腳本沒關分頁會拖垮 CDP Chrome（砍掉 `buyer-match-chrome` 相關
`chrome.exe` 重開即可，登入態不會掉）。

**刻意沒做**LINE 推播／排程。分支 `claude/buyer-match-yc-link`，第四支 commit 還沒開 PR。

### 4. 公開網頁在 Cloudflare Pages（2026-08-05 上線，發布鏈路見 `reference.md`）

- 三合一計算機 https://yc-tools.pages.dev/yc-calc/
- 行情看板 https://yc-tools.pages.dev/kh-market/
- 買方配案看板（內部用，`robots.txt` 擋收錄）https://yc-tools.pages.dev/buyer-match/

⚠️ **`qingan3-calc` 帳號恢復了但還是 404**：GitHub 讀取被擋那個技術性障礙已解除，
問題回到本質——**沒有本機原始檔**，要救得先找到原始檔才動得了。

---

## 其他待辦

- **Notion「來源連結」欄已刪（2026-08-05）**：存的是會過期的 houseol 網址（刪除前 72 筆全部 404）。
  引用全改指 `永慶官網連結`（線 B 二次確認／`publish.py`／yc-ad skill）。**不要再加回來**，理由見 `reference.md`
- **Notion v2 舊資料**：YC1868705 還在、不在 KEIS，線 B 掃到略過；它的 `永慶官網連結` 已補上
- **公買搶單備註漏寫已修**（08-08，經過見 `incidents.md`）：每天自動跟 KEIS 對帳，`--audit-remarks` 可手動跑
- **公買搶單 08-14**：型態排除清單加「土地」（`EXCLUDE_TYPES`，原本只排公寓）；
  拿掉第三個帳號許育禎（`.env` 少了 `KEIS_USERNAME3`），現在薛力瑜＋周珈伊兩帳號、共 14 配額。
  另加：需求區域排除美濃/六龜/岡山/路竹/大寮/橋頭/杉林/燕巢/林園/梓官區（`EXCLUDE_DISTRICTS`）；
  新增 `--record`（只全池掃描記錄總帳、不申請）給空窗天用——空窗 <7 天現有判斷本來就安全，
  ≥7 天回來前先跑一次補紀錄再開 `--watch`。repo／桌面 `grab.py` 已同步
- **未來候補**：多開一個 IG 帳號專發廣告。使用者要先自己把新 IG 轉商業帳號、綁粉專＋開權限，
  之後才接 n8n（IG 是兩段式 container→publish）。目前卡在帳號還沒建，不用主動催
