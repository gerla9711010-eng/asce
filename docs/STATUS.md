# STATUS — 現在的狀態與接下來要做的事

> **這份只放「現況」和「待辦」。完成的項目直接刪掉，不留歷史。**
> 查表（入口/credential/Notion 欄位/指令/工具清單）→ `reference.md`
> 事故經過與血淚史 → `incidents.md`
> 線上真正在跑什麼 → `n8n-live.md`（n8n_sync.py 產的，**兩邊打架信這份**）

最後更新：2026-08-04 ｜ 使用者：薛力瑜（永慶不動產 博愛凱璿加盟店）

---

## 🔴 開工第一件事：GitHub 帳號停權中（2026-07-30 11:47 起）

`git push` / `gh pr merge` 一律回 403 `Your account was suspended`。
**不要浪費時間重試推送，收工一律只做本機 commit，跳過 push/PR，等帳號恢復再一次補推。**

- **未推的東西**：本機分支 `claude/multi-store-price-fix-0730` 有約 28 個 commit 未推；
  GitHub 上 **PR #161 開著未合**。恢復後：`git push` → 合 PR #161 → 把後續 commit 一起併進 main
- **離線備份**：`桌面\asce-備份-20260730.bundle`（完整歷史，`git clone` 那個檔就能還原）
- **申訴**：2026-07-30 21:31 已送工單（support.github.com），等 GitHub 客服回信，不用重複申訴
- 已設每天自動檢查帳號有沒有恢復，恢復會主動通知
- 排程與桌面工具**完全不依賴 GitHub**，停權期間照跑
- 觸發原因與往後的 PR 節奏 → `incidents.md`

---

## 🔴 三條永久紅線（動之前先看 `incidents.md`）

1. **Railway `EXECUTIONS_DATA_SAVE_ON_SUCCESS` 一律保持 `all`**
   改 `none` 會讓所有含 Wait 節點的流程無聲死掉（煞車 10 分鐘就是 Wait）。
   空間不夠的正確解法是調降 `MAX_AGE`，不是關掉紀錄。
2. **AI 回傳壞掉時寧可不發，絕不硬發**
   保底邏輯只要「讓流程繼續」而不管「送出去的東西對不對」，就是在把故障洗成看起來成功的樣子。
   對外送出的東西一律要有形狀檢查。
3. **KEIS `show_in_web=false` 的案子絕對不發**（屋主/門市沒同意網路露出）。擋在 `篩選候選` 最上游。

**另外兩條通則**：監控不准住在 n8n 裡（見廣告發文看門狗）；改完一定要在「真正在跑的環境」實地驗一次。

---

## 系統現況

| 線 | 狀態 |
|---|---|
| 線 A 掃描發文（09/11/13/15/17/19）／線 B 下架偵測（08:00、22:00）／線 D KEIS 同步 | 🟢 active |
| 線 C 重發輪替（09:30～19:30 每 2 小時）| 🟢 active；**FB 發/刪那段還沒真的跑過** |
| 煞車（停）／ LINE 指令分流器 ／ 靜默失敗巡邏 | 🟢 active |
| 系統錯誤 LINE 告警 | 🟢 active（**必須保持 active**，否則不轉發）|
| 廣告發文看門狗（桌面，n8n 外，每小時）／公買搶單／自動簽到／買方配案排程 | 🟢 上線中 |

---

## ▶ 下次開工從這裡接

### 0. ★★ 文案交叉審核：做好了但**還沒上線**，等驗完誤擋率（2026-08-04）

發文前多一道「第二個模型當路人看一遍」，抓數字守門員抓不到的語意問題（混到別間物件、前後矛盾）。

```
python scripts/pending/verify_copy_review.py 16      # ① 先驗誤擋率
python scripts/pending/apply_copy_review.py --deploy # ② 過關才部署（含 deactivate+activate）
python scripts/n8n_sync.py                           # ③ 讓 git 跟 n8n 對齊
```

**為什麼還沒上線**：擋錯一篇＝整班廣告沒發，誤擋率是上線門檻。第一版 14 篇擋 12 篇，
三個成因都修好了（同一批前 4 篇從 0/4 變 3/4），但 Gemini 每日配額用完、樣本不夠不敢開。
**門檻＝零誤擋，或每個誤擋理由都站得住腳。** 踩過的坑寫在 verify 腳本開頭。

### 0.5 🔴 Gemini 免費配額每天只有 20 次（每個模型），系統本來就貼著上限在跑

2026-08-04 實測出來的硬天花板，細節見 `reference.md`。線 A 6 班 + 線 C 6 班 + LINE 三個指令
合計已經接近 20，**使用者多打幾次「客戶」「行事曆」就會讓廣告線在 `產文案` 靜靜失敗**。
要根治：分散到不同模型，或去 Google Cloud 開帳單轉付費方案（用量這麼小，一個月不到一杯飲料）。
**這件事跟交叉審核無關，就算不做審核也該處理。**

### 0.8 KEIS 被砍的欄位都回來了，但**先別改回去**

2026-08-04 實測：`images`／`age`／`school_info`／`official_url`／`layout`／`floor_info` 又有值了。
現在的繞道做法比較耐打，KEIS 砍過一次就會砍第二次。詳見 `reference.md`。

### 1. ★ 守門員誤擋要修（2026-07-30 已診斷完，使用者說「過兩天再處理」）

當天 4 班的實況：09:00 ✅、11:00 ✅、13:00 一件被擋→自動換下一件發出、15:00 三件全沒過→沒發。
**FB API 沒拒絕過、n8n 0 錯誤**，全部是自己攔的。四件裡三件是誤判：

| 物件 | 系統說 | 真相 | 要改哪裡 |
|---|---|---|---|
| EG0501404 鳳山紫金城 | 不該提「學區」 | 文案寫的是「O14鳳山國中**捷運站**」，站名不是學區 | 判學區前先排除捷運站名，或改成只有出現「學區/就讀/劃分」等語境詞才算宣稱 |
| YG0134508 岡山鳳凰信義 | 不該提「樓層」 | KEIS 寫 `14-13/14`、官網寫 `13/14` → 交叉檢查判不一致 → 樓層被清空 → 文案有「次高樓層」就被擋 | 不一致時改**採用官網值**（公開可驗證）而不是清空；或放行不含數字的相對描述 |
| EG0529474 榮總時代爵邸 | 官網查不到連結 | **官網有**：`house/7457619` productID `TC0529474` 完全對上。當班抓到的是空殼頁（名/社/路/價全 X＝內容沒載到），因為那是當天剛上架的新頁 | ①候選頁全讀不到 productID 時當「抓失敗」重試，不要當「不是這件」 ②`if (!cands.length && community)` 改成「編號搜尋的候選全比不上時也退回社區名搜尋」——當天搜「時代爵邸」21 筆裡就有正確那筆 |

兩件被擋的已在 Notion（狀態=`草稿`、文案都產好了、備註寫原因），要發就改成「待發」；
**系統不會自己再試那兩件**。EG0529474 那件沒建 Notion，每班都會自動再試。

### 2. AG1911938 鳳山鳳松路透天：過兩天再看

KEIS 資料完整、編號有效（1580 萬／空屋），但**官網真的還沒有**（編號差 1 號的 AG1911937 有
→ 不是搜尋壞掉）。KEIS 沒有「是否上網／屋主同意刊登」欄位，只能看「連續幾天都查無 = 大概真的不上網」。
⚠️ 坪數**不是填錯，別去問店家**：土地 19.36／建物登記 14.266 坪、賣點寫「另增建」＝有加蓋，
單價 110 萬/坪 是拿登記坪當分母才顯得離譜。

### 3. 三段還沒被真實事件走完的流程（都各自驗過了，等自然發生）

- **FB 補救分支**：只單獨驗過兩個端點，等 FB 真的打嗝。看到 LINE 推「❌ 廣告沒發出去」就去看該班 `判斷補救方式` 的 `mode`
- **線 C 的 FB 發新文＋刪舊文**：沒人勾過 `要重發` 所以每班 0 件。要測就把任一筆已發布物件的 `要重發` 打勾
- **線 B 完整一條**：等下一個物件自然下架。⚠️ 真有案件下架時**順便看一件事**（使用者交代）：
  進 KEIS 廣告追蹤看「永慶官網連結失效」在頁面上長什麼樣，再決定下架偵測要不要改以 KEIS 為準。**現在不要預先做**

### 4. 買方配案剩下的小事

- **⏰ 2026-08-11 後刪掉 `C:\Users\user\_待刪_20260804_買方配案舊登入態\`（4.8 GB）**
  兩份停用的 Chrome 登入態，08-04 搬出來但沒刪、觀察一週。刪前看 `daily_run.log` 有沒有正常跑。
  徵兆是 LINE「🔑 房地要重新登入」→ 掃一次 QR Code 即可，不用搬回來。細節見記憶 buyer-match-chrome-profiles。
- 車位型態（平面式/機械式）還沒對應，目前只做到「有/無車位」

---

## 🔴 未解：桌面 grab.py 跟 repo 兩邊各有對方沒有的東西（2026-08-04 收工發現）

**不要單方向複製，會毀掉真的東西。** 差 151 行，而且是雙向分岔：

| 只有桌面有 | 只有 repo 有 |
|---|---|
| `WINDOW_SIZE = 70`（repo 還是 40，08-03 因為爆量 32 筆漏搶兩筆才改的）| 08-01 起 6 個 commit 的強化 |
| 縣市欄位空白＝當高雄市放行（08-03，空白常是漏填不該擋）| 心跳繞過 n8n 直推 LINE（`KEIS_LINE_DIRECT_TOKEN`）|
| 漏接偵測（被視窗排除的合格名單會 LINE 通知）| 總帳每日備份／當日計數落地／帳號健檢／開盤守門／API 欄位體檢 |

**真正在跑的是桌面那份**，所以 08-03 那三項搶單邏輯是現行規則；repo 那 6 個 commit 則從沒上過線。
桌面版已備份到 `backup/keis-desktop-snapshot-20260804/grab.py`（gitignore，不會進版控）。

**要怎麼收**：把桌面獨有的三項合進 repo 版 → 產出一份合併版 → 覆蓋桌面 → 實跑一天確認。
⚠️ 動之前先確認沒有別的 session 正在改 grab.py（08-04 當天就有兩個對話同時在動它）。

## 其他待辦

- **Notion 3 筆 v2 舊資料**（YC1868705 / YE0095535 / YC1835328）不在 KEIS，線 B 每天掃到就略過。
  YC1835328 已判定下架（那筆沒有粉專貼文，不會刪到東西）
- **LINE「戰果」最終驗收**：需使用者拿手機打一次（需真實 replyToken，模擬不了）
- **n8n 有兩個同名「LINE 指令分流器」**，舊的已停用未刪，跑幾天沒問題後刪掉舊的
- **公買搶單對帳未驗的一半**：周珈伊 7 筆需登入她帳號才能核（密碼類使用者自己登入）
- **未來候補**：多開一個 IG 帳號專發廣告。使用者要先自己把新 IG 轉商業帳號、綁粉專＋開權限，
  之後才接 n8n（IG 是兩段式 container→publish）。目前卡在帳號還沒建，不用主動催
