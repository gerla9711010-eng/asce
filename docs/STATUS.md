# STATUS — 現在的狀態與接下來要做的事

> **這份只放「現況」和「待辦」。完成的項目直接刪掉，不留歷史。**
> 查表（入口/credential/Notion 欄位/指令/工具清單）→ `reference.md`
> 事故經過與血淚史 → `incidents.md`
> 線上真正在跑什麼 → `n8n-live.md`（n8n_sync.py 產的，**兩邊打架信這份**）
> 外部 AI 工具情報（評估過的／已排除的，不用重評）→ `ai-toolbox.md`

最後更新：2026-09-19 ｜ 使用者：薛力瑜（永慶不動產 博愛凱璿加盟店）

---

**PR 節流**：不管幾個視窗同時開，一天全部加起來最多 1 個 PR（三次停權換來的，別放寬）。

---

## 🟢 公買搶單存活檢查已接上（09-18 確認 58 筆「正常」，細節＋`run.bat` 禁忌見 `reference.md`）
「07:00 就開搶」09-14 查證後**決定不做**（缺口只有 9 分鐘，理由同上檔）。

---

## 🟢 廣告調價/下架偵測修好（09-05 上線）

「下架／降價／漲價」標籤是 KEIS 自己爬官網打的，我們只讀。09-05 修掉三個洞（見 `incidents.md`），
另加 `KEIS 價格沒跟上?` 保險絲擋原價重發的無限迴圈。

⚠️ **09-06 那班驗收沒人回頭確認過**（該刪2篇臉書貼文、該重發7件、AG1786755該用官網現價880），
下次開工先查 KEIS 廣告追蹤頁跟臉書有沒有照這樣跑完，跑完了這條就整段刪掉。

## 🟢 Codex 文案通道／Telegram 告警（細節見 `reference.md`）

通道網址每次重開都變，**看 `n8n-live.md`**，別寫死。`incident-watch/watch.py` 常駐每 15 分自動修。
⚠️ 它只查 `status=error`，**被容錯吞掉的失敗看不到**（那類靠 n8n `靜默失敗巡邏` 發 🔕 告警）。
⚠️ 煞車「停 XXX」只有 LINE 指令分流器收得到，Telegram 喊不了停。
⚠️ 裝完自動化要去 `shell:startup` 實地看一眼（08-29～09-02 空跑 4 天）。

---

## 🔴 三條永久紅線（動之前先看 `incidents.md`）

1. **Railway `EXECUTIONS_DATA_SAVE_ON_SUCCESS` 一律保持 `all`**
   改 `none` 會讓所有含 Wait 節點的流程無聲死掉。空間不夠要調降 `MAX_AGE`，不是關掉紀錄。
   清執行紀錄一律 **TRUNCATE**，絕不用 API 批次 DELETE（08-09 停機）。
2. **AI 回傳壞掉時寧可不發，絕不硬發**——對外送出的東西一律要有形狀檢查。
3. **KEIS `show_in_web=false` 的案子絕對不發**（屋主/門市沒同意露出）。擋在 `篩選候選` 最上游。

**另外兩條通則**：監控不准住在 n8n 裡；改完一定要在「真正在跑的環境」實地驗一次。

---

## 系統現況（2026-08-25 用 Public API 實地核對過）

| 線 | 狀態 |
|---|---|
| 線 A 掃描發文／線 B 下架偵測／線 C 重發輪替 | 🟢 全 active，08-25 兩班驗證真的發文成功，含發文預告＋成功推播 |
| 系統錯誤告警／搶單通知／工作回報查詢／簽到通知（後者只寫日誌不推） | 🟢 已開 |
| 煞車（停）／LINE 指令分流器／圖片分流器／客戶建檔器／行事曆建立器／戰果查詢／查專員電話 | 🟢 已開（LINE 觸發，沒時鐘）|
| 情資週報／靜默失敗巡邏／KEIS 待聯絡提醒 | 🟢 08-25 全改推 Telegram；待聯絡提醒 09-05 修好幽靈名單 |
| 廣告發文看門狗（`ad_watchdog.py`）／incident-watch | 🟢 09-02 重開；09-14 加搶單存活檢查 |
| 公買搶單（門市電腦 grab.py） | 🟢 09-05 19:53 重開。高頻窗口 07:59-08:03、全池掃描 4 小時 |
| YC 建檔器 v3（鎖物編+總價） | ⚪ 已停（唯一保留的舊版，留著當防幻覺邏輯參考）|
| ~~LINE 新聞推播／YC 建檔器 v2／YC 發文線／文案重產器~~ | 🗑 09-15 從 n8n 刪除；要取回 JSON 用 `git show fa81093^:workflows/<檔名>`（`backup/` 那份只在本機） |
| 高雄實價登錄行情看板每日更新（`KH-Market-AutoUpdate`） | 🟢 09-15 重新啟用（本來停用超過一個月沒人發現）；已接聚合腳本自動重算，資料邊界自動偵測 |

「LINE 推播 → Notion 系統日誌」的架構細節見 `reference.md`。

### 🟢 廣告線 09-16 已恢復，五班全發（09-15 那兩個故障的經過見 `incidents.md`）

**現在誰在產文案**：`gemini-fallback`。**10/01 13:00 Codex 額度恢復後自動換回去，不用動手。**
在那之前 Gemini 同時扛產文案＋交叉審核，容易撞 503（重試已放寬，10/01 後也不用改回來）。
⚠️ 觀察點：交叉審核若一直 SKIP，「賣點有沒有編造」那層會漏（數字守門員不受影響）。
連續兩天都 SKIP 再考慮改成審核失敗就不發。

## 🟢 線A 發文條件加上「型態＋屋齡」分層（09-19 上線，使用者定案）

只發 **大樓／透天／華廈**（土地、車位、其他一律不發）；**屋齡 ≤25 年且總價 ≥1000 萬優先**，
其次同屋齡的 800~999 萬，**再湊不滿 3 件就從最新的往下遞補（型態仍要對、屋齡不限）**。
型態／屋齡只有單筆詳情 API 有（列表 API 沒有也不吃篩選參數，09-19 實測），所以判重後多了
「預篩上限 20 件 → KEIS 詳情預篩 → 型態屋齡分層」三節點。⚠️ 整批抓不到那兩欄＝KEIS 又砍欄位
→ 推 Telegram 並**退回改版前行為照舊發**，不整班空轉。實測 69 件候選→合格 30 件，約 7~8 件/天。
⚠️ **19:41 上線還沒跑過真實班次**：看 09-20 09:00 那班 execution 有無 success、發的是不是合格案，確認完刪掉這行。

---

## ▶ 下次開工從這裡接

### 0.1 守門員：改的時候只能「補真出處」不能「放寬檢查」（見 `incidents.md` 第 10 個 bug）

**已決定不做**：煞車縮短、文案純套版、Google Cloud 開帳單、交叉審核換 Codex（理由見 `reference.md`）。

### 0.8 KEIS 被砍的欄位（`images`／`age`／`school_info`…）已修復，**先別改回去**——見 `reference.md`

### 1. 🟢 AG1911938 鳳山鳳松路透天：官網已上架 `house/7457882`，等下一班線A自己撈到發文
（坪數不是填錯別去問店家，有加蓋，110萬/坪是登記坪當分母才顯得離譜）

### 1.2 🟢 屋主銷售報告書已工具化（09-18 首件）

新 skill `.claude/skills/sale-report/`：丟資料夾（建物謄本 PDF＋104woo 實登 PDF＋周遭開價截圖）
→ 13 頁可編輯 PPT（原生表格／圖表，非生成圖片）。首件 `市中一路458巷7-4號`，資料留在
`example.json`，複製改欄位就是下一件。輸出在 `桌面\DM\銷報書\<物件>\`。
兩條規則已寫進 SKILL.md（使用者裁示，不用再問）：①**開價圖上不會有本案**——會需要銷報書就代表
屋主還沒定價，別猜哪個 pin 是；②**土地持分拿不到就整欄留空白**，不寫「待查」不寫解釋，也不拿它推價格。

### 1.5 🟡 買方配案系統（09-21 補完 43 客需＋新增雙擊 GUI，GUI 還沒被真人登入實測過）

`scripts/buyer-match/`：手動貼 Console 的流程仍在（`collect.js` → `FDBM.run()` → `dumpState()` →
`build_page.js`），09-21 全 43 客需、423 筆官網連結補完，含首跑漏的 `79842黃敏哲透天`、`杏湖社區`。
撞查詢上限的提示條「超過查詢次數限制…」腳本會自動偵測停手不存半成品；`MAXOPEN` 8→30；
`resultCount()` 認得「找不到符合的物件」避免誤判成撞上限（杏湖社區踩過的坑）。

**新增**：桌面雙擊 `買方配案更新.bat` → 開 `scripts/buyer-match/gui.py`（tkinter），按「開始更新」
自動跑完 `worker.py`（Playwright 開獨立瀏覽器 profile `browser-profile/`）→ 收集 → 產頁 → 覆蓋桌面
`買方配案.html`，全程不用找 Claude。**只跑到「開瀏覽器、正確顯示要求登入」這步用自動化測過，
真人登入後的完整收集流程還沒被實測**，第一次用要盯著看有沒有正常跑完。
`worker.py` 的登入判斷、撞上限判斷、頁面重整重試都是照 `collect.js` 的邏輯抄的，理論上一致。
**不要排程自動觸發，維持人手動雙擊。** ⚠️ 含客戶姓名，`state-*.json`/`data.json`/`*.html`/
`browser-profile/` 已 gitignore。

### 2. FB 補救分支還沒被真實事件走完：收到「❌ 廣告沒發出去」就看該班 `判斷補救方式` 的 `mode`

### 3. 公開網頁（發布鏈路見 `reference.md`）：https://yc-tools.pages.dev/yc-calc/ ｜ `/kh-market/`
⚠️ `qingan3-calc` 還是 404：沒有本機原始檔，要救得先找到原始檔。

---

## 其他待辦

- 🔴 「疑似同業每日網頁比對」（08:30）**查無≠乾淨**：WebSearch 查不到手機 Google 秒出的仲介名片頁
  （驗收基準＝0989808766/許聖賢）。09-04 已申請 Google CSE（key/cx 進兩份 `keis/.env`），
  **但一律 403 `does not have the access`**，疑生效延遲，下次 session 先重測。
  另待解：①雲端 routine 讀不到本機 .env ②搜尋範圍 2027-01-01 起只能指定網站，待問落在哪個網域。
- **待核對**：08-26 那批 36 筆「新進池」記成 grab.py 停機 6 天漏記的舊單，跟 `incidents.md`
  「腳本其實一直活著」矛盾，確認哪份記錯
- **Notion v2 舊資料**：YC1868705 還在、不在 KEIS，線 B 掃到略過；`永慶官網連結` 已補上
- **未來候補**：多開一個 IG 帳號專發廣告。使用者要先自己把 IG 轉商業帳號＋綁粉專開權限，
  之後才接 n8n。卡在帳號還沒建，不用主動催
- **09-15 夾帶別的視窗未完成的改動**（[[parallel-claude-sessions]]，本次沒動邏輯只是一起 commit）：
  `buyer-match/collect.js` v5→v6（擬人化隨機停頓＋預設慢3倍 `SLOW`，`run({slow:N})` 可調）、`clockin.py` 加 Telegram 直推——都還沒實測過
