# STATUS — 現在的狀態與接下來要做的事

> **這份只放「現況」和「待辦」。完成的項目直接刪掉，不留歷史。**
> 查表（入口/credential/Notion 欄位/指令/工具清單）→ `reference.md`
> 事故經過與血淚史 → `incidents.md`
> 線上真正在跑什麼 → `n8n-live.md`（n8n_sync.py 產的，**兩邊打架信這份**）
> 外部 AI 工具情報（評估過的／已排除的，不用重評）→ `ai-toolbox.md`

最後更新：2026-09-05 ｜ 使用者：薛力瑜（永慶不動產 博愛凱璿加盟店）

---

## 🟢 廣告調價/下架偵測修好（09-05 上線）

KEIS 廣告追蹤的「案件下架／降價／漲價」標籤是 KEIS 自己爬官網打的，我們只讀。
線 B 讀到後刪文或勾「要重發」，線 C 再刪舊文重發。09-05 修掉三個洞（完整經過見 `incidents.md` 最上面）：
廣告清單 `limit=100` 沒翻頁（184 筆只看到 100 筆，5 筆降價/漲價從沒被檢查過）、
Notion `page_size:100` 同病、漲價完全沒接。另加 `KEIS 價格沒跟上?` 保險絲擋原價重發的無限迴圈。

**等下一班驗收**（09-06 08:00 線 B → 09:30 起線 C，一班一件）：
- 會刪 2 篇臉書貼文：AA1069194 岡山勵志新城、PG0092558 全新未住絕版轉讓（官網連結已死）
- 會排 7 件重發：降價 5 件（美濃 700 萬最大）＋漲價 2 件（YA0051271、AA1044536）
- AG1786755 會用**官網現價 880** 重發（KEIS 物件管理還停在 950 沒跟上）——09-05 晚上已改成「價格一律以官網那頁為準」，不用等人去 KEIS 改

---

## 🔴 GitHub 帳號又被停權（08-26 15:32）——收工流程暫時只做本機 commit

`gh pr merge 235` 當下噴 403「Your account was suspended」。`git fetch` / `push` / `gh` 全部不能用。
成因跟 2026-07-30 同一個：**PR 開太密**（08-25 晚 4 個 ＋ 08-26 下午 3 個）。

**恢復前的做法**：branch → PR → merge 整段跳過，只做本機 `git commit`。不用重試、不用等。
- `f839c97`（.bat 說明）已在本機 main，遠端狀態不明——PR #235 開出來了，合了沒不知道。
- 恢復後第一件事：`git fetch`，比對 #234/#235 有沒有進 origin/main，再決定要不要重推。
- **新節制標準：一個工作天最多 2 個 PR**，跨 session 也算。
- **申訴已送出**（08-26 20:07，support.github.com → Reinstatement request → GitHub account or
  content），**Ticket ID: 4700948**。已收到自動確認信，客服量大要等，不用重複送、不用催。
- 09-01、09-03 確認：`git fetch` 仍 403，同一次停權還沒解。已建雲端排程「GitHub帳號停權檢查」
  （每天 09:00，用 WebFetch 查 github.com 頁面＋Notion 記狀態，**只有狀態改變才推播**，
  不會每天重複吵），解封那天會自動推播通知，不用手動盯。

---

## 🟢 Codex 文案通道：08-29 修好「重開失敗後卡死不再重試」的 bug

線A `Gemini 產文案` 目前指向哪個通道網址**看 `n8n-live.md`**（每次重開都會變，別寫死在這）。
驗活的方法：對通道 POST `{}` 帶 `X-Codex-Token` → 回 **400「text 是空的」＝活的且密鑰對**，
401 是密鑰錯，502/530 是通道死了。

🟢 **`scripts/incident-watch/watch.py`**：背景常駐，每 15 分鐘查 n8n 執行紀錄，撞到「通道死掉」
會自動修+發 Telegram 報告；09-02 已補裝開機啟動並確認在跑。
⚠️ 裝完自動化要去 `shell:startup` 實地看一眼，別只寫「已裝」（08-29～09-02 就空跑了 4 天）。

---

## 🟢 系統通知全改走 Telegram（08-19 起，08-25 做完）

LINE 200 則配額留給客戶群發，系統告警／提醒全搬去 Telegram（無上限）。LINE 關鍵字回覆
（情資／戰果／工作回報…）不受影響。細節見 `reference.md`「補回主動推」，經過見 `incidents.md`。

⚠️ 煞車「停 XXX」只有 **LINE 指令分流器** 收得到，**要 Telegram 也能喊停＝另外做一支 Telegram trigger**。
⚠️ 批次啟用/遷移前先查 `incidents.md` 有沒有記過某支 workflow 是死的（心跳檢查踩過兩次假警報）。

**還沒被真實事件驗過**（結構已對，等自然發生，收不到訊息是正常的）：靜默失敗巡邏（24h 內
同錯不重報）、KEIS 情資週報的異常告警分支。

---

## 🔴 三條永久紅線（動之前先看 `incidents.md`）

1. **Railway `EXECUTIONS_DATA_SAVE_ON_SUCCESS` 一律保持 `all`**
   改 `none` 會讓所有含 Wait 節點的流程無聲死掉（煞車 10 分鐘就是 Wait）。
   空間不夠的正確解法是調降 `MAX_AGE`，不是關掉紀錄。
   清執行紀錄一律用 **TRUNCATE**，絕不用 API 批次 DELETE（08-09 停機經過見 `incidents.md`）。
2. **AI 回傳壞掉時寧可不發，絕不硬發**
   保底邏輯只要「讓流程繼續」而不管「送出去的東西對不對」，就是在把故障洗成看起來成功的樣子。
   對外送出的東西一律要有形狀檢查。
3. **KEIS `show_in_web=false` 的案子絕對不發**（屋主/門市沒同意網路露出）。擋在 `篩選候選` 最上游。

**另外兩條通則**：監控不准住在 n8n 裡（見廣告發文看門狗）；改完一定要在「真正在跑的環境」實地驗一次。

---

## 系統現況（2026-08-25，用 Public API 實地核對過）

| 線 | 狀態 |
|---|---|
| 線 A 掃描發文／線 B 下架偵測／線 C 重發輪替 | 🟢 全部 active，FB 權杖問題已解決，08-25 17:00／19:00 兩班驗證真的發文成功；08-25 晚補回發文預告＋發文成功 Telegram 推播 |
| 系統錯誤告警／搶單通知（08-25 已補回 Telegram 主動推，系統日誌照寫）／工作回報查詢／簽到通知（後者維持只寫日誌，不推） | 🟢 已開 |
| 煞車（停）／LINE 指令分流器／圖片分流器／客戶建檔器／行事曆建立器／戰果查詢／查專員電話 | 🟢 已開（LINE 觸發，沒時鐘）|
| 情資週報／靜默失敗巡邏／KEIS 待聯絡提醒 | 🟢 08-25 全部改推 Telegram＋重新啟用；待聯絡提醒 09-05 修好「Notion filter 回舊索引→推幽靈名單」並加上同電話前筆標示（見 incidents.md 最上面那則）|
| 廣告發文看門狗（`scripts/keis/ad_watchdog.py`）／incident-watch | 🟢 09-02 重新開啟：看門狗 Task Scheduler 改回 Enabled（每小時查 Notion，走 Telegram）；incident-watch 補裝開機啟動。一開就抓到 `AG1786752` 卡在「待發」23 天（08-10～09-02，KEIS廣告ID/粉專連結全空，沒建過任何廣告），已刪除該筆 Notion 紀錄 |
| 自動簽到／市場週報／行情看板更新 | ⚪ 已停（沒動）|
| 公買搶單（門市電腦 grab.py） | 🟢 09-05 19:53 重新開啟（09-04 11:25~09-05 19:53 停機 32 小時，損失 3 筆合格名單 90328/90326/90324）。高頻窗口已收窄成 07:59-08:03、全池掃描改 4 小時 |
| YC 建檔器 v2/v3／YC 發文線／文案重產器／LINE 新聞推播 | ⚪ 已停（舊版，沒動）|

「LINE 推播 → Notion 系統日誌」的架構細節見 `reference.md`。

---

## ▶ 下次開工從這裡接

### 0.1 守門員：改的時候只能「補真出處」不能「放寬檢查」（第 10 個 bug 經過見 `incidents.md`）

**已決定不做**：煞車縮短、文案純套版、Google Cloud 開帳單、交叉審核換 Codex（理由見 `reference.md`）。

### 0.8 KEIS 被砍的欄位（`images`／`age`／`school_info`…）已修復，**先別改回去**——見 `reference.md`

### 1. 🟢 AG1911938 鳳山鳳松路透天：官網已上架 `house/7457882`，等下一班線 A 自己撈到發文
⚠️ 坪數**不是填錯，別去問店家**：有加蓋，單價 110 萬/坪 是拿登記坪當分母才顯得離譜。

### 1.5 ⏸️ 買方配案系統（09-02 首跑成功，**使用者喊暫停，等他想清楚再動**）

`scripts/buyer-match/`：貼 `collect.js` 進房地物件頁 Console → `FDBM.run()` 掃 A/B/C 買資料夾
→ `FDBM.dumpState()` → `node build_page.js` 產彙總頁。09-02 首跑 43 客需、810 筆官網連結。
⚠️ **跑到後段撞到房地「超過查詢次數限制」**，`79842黃敏哲透天`(20筆)、`杏湖社區` 兩個沒抓到。
**暫停中：不要自動排程、不要主動重跑。** 恢復後才做（額度回復後）：`FDBM.run({only:['79842黃敏哲透天','杏湖社區']})` → 重產頁面 →
**記得一併覆蓋 `桌面\買方配案.html`**（兩份會各自過期）。
⚠️ 資料含客戶姓名：`data.json` / `*.html` 已 gitignore，**只留本機、不發公開連結**。

### 2. 兩段還沒被真實事件走完的流程（都各自驗過了，等自然發生）

- **FB 補救分支**：等 FB 真的打嗝。收到「❌ 廣告沒發出去」就看該班 `判斷補救方式` 的 `mode`
- ~~線 C 的 FB 發新文＋刪舊文~~ 已走完（EG0506900、AG1786755 都真的重發過），改看上面 09-05 那則的驗收清單

### 3. 公開網頁在 Cloudflare Pages（發布鏈路見 `reference.md`）

- 三合一計算機 https://yc-tools.pages.dev/yc-calc/ ｜ 行情看板 https://yc-tools.pages.dev/kh-market/

⚠️ **`qingan3-calc` 還是 404**：沒有本機原始檔，要救得先找到原始檔才動得了。

---

## 其他待辦

- 🔴 「疑似同業每日網頁比對」（08:30）**查無≠乾淨**：WebSearch 查不到手機 Google 秒出的仲介名片頁
  （驗收基準＝0989808766/許聖賢）。09-04 已申請 Google CSE，key＋cx 進兩份 `keis/.env`（`GOOGLE_CSE_*`），
  API 已啟用、配額 100/天，**但一律 403 `does not have the access to Custom Search JSON API`**（重試 6 次，
  新版 UI 無「不限制金鑰」可拆），疑生效延遲，**下次 session 先重測**。另待解：①雲端 routine 讀不到本機
  .env ②「搜尋整個網路」2027-01-01 停用只能指定網站（暫填 `*.591.com.tw`，待問使用者那筆落在哪個網域）。
- **待核對**：08-26 那批 36 筆「新進池」記成 grab.py 停機 6 天漏記的舊單，跟 `incidents.md`
  「腳本其實一直活著」矛盾，確認哪份記錯
- **Notion v2 舊資料**：YC1868705 還在、不在 KEIS，線 B 掃到略過；`永慶官網連結` 已補上
- **未來候補**：多開一個 IG 帳號專發廣告。使用者要先自己把 IG 轉商業帳號＋綁粉專開權限，
  之後才接 n8n。卡在帳號還沒建，不用主動催
