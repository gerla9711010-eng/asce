# STATUS — 現在的狀態與接下來要做的事

> **這份只放「現況」和「待辦」。完成的項目直接刪掉，不留歷史。**
> 查表（入口/credential/Notion 欄位/指令/工具清單）→ `reference.md`
> 事故經過與血淚史 → `incidents.md`
> 線上真正在跑什麼 → `n8n-live.md`（n8n_sync.py 產的，**兩邊打架信這份**）
> 外部 AI 工具情報（評估過的／已排除的，不用重評）→ `ai-toolbox.md`

最後更新：2026-08-24 ｜ 使用者：薛力瑜（永慶不動產 博愛凱璿加盟店）

---

## 🟢 FB 發文已修復（08-24 22:56），**明早 09:00 那班要回頭看**

08-19 15:12 ～ 08-24 全停，5 天一篇沒發。經過見 `incidents.md`。已做完：
- Meta 後台把 `n8n-bot` 的粉專資產權限從「部分管理（內容）」改回 **完整管理**
- 重新產生系統使用者權杖（永不過期）→ 換成粉專權杖 → 存進 n8n credential `FB Page Token`
- 實測 `GET /1041868522352339?fields=name` 回傳粉專名稱 ✅
- 線 A/B/C 都是 active

**還沒驗到的**：實際發文（`pages_manage_posts`）。讀取已通、權限也勾了，但沒有真的發過一篇。
**明天 09:00 線 A 那班**看最後一個節點是不是 `Notion 記 KEIS 廣告ID`；如果又是 `記錄發文失敗`，
看錯誤碼是不是 `(#200) permission denied`（那就是粉專資產權限還沒生效）。

⚠️ **踩過的坑**：n8n Header Auth 的 Value 必須是 `Bearer ` **加一個空格**再接權杖。
少了 `Bearer ` 時 FB 回的是 `(#200) Provide valid app ID` / `code 2500`，
**看起來像權杖壞掉，其實只是少了前綴**。

⚠️ **順便補洞**：`記錄發文失敗` 只寫 Notion 日誌沒推播，才會連錯 5 天沒人知道——Telegram 遷移時一起加。

---

## 🟠 系統通知改走 Telegram（08-19，**做到一半，下次開工第一件事**）

**為什麼**：LINE 官方帳號免費方案一個月只有 200 則主動推播，那 200 則要留給客戶群發。
Telegram 機器人推播沒有則數上限，系統告警／提醒一律搬過去。LINE 的**關鍵字回覆
（情資／戰果／工作回報…）完全不動**——reply 本來就不吃額度。

⚠️ **更正一個先前的誤判**：n8n 現在幾乎沒在吃 LINE 額度，因為會 push 的三支（靜默失敗巡邏／
待聯絡提醒／心跳檢查）從 08-12/13 停機後就一直是關的。額度是**後台手動群發客戶**用掉的。
真正的問題是「為了省額度不敢開監控」，換成 Telegram 就沒這個顧慮。

**已完成**
- Telegram 機器人 `@Keis_ace_bot`（顯示名「業務助理」），收訊 chat id `1890720012`
- n8n credential「Telegram 業務助理 Bot」（`yBF20qXez1b7FLFI`，type `telegramApi`）已建好
- `scripts/keis/notify_telegram.py`（新）；`ad_watchdog.py` 的 `push_alert()`、`grab.py` 的
  `push_direct()` 改成**先送 Telegram、LINE 當備援**。repo 與桌面 `keis/` 兩邊已同步、實測送達
- token 放各自的 `.env`（在 `.gitignore` 裡，不會進 git）

**待做**
1. 跑 `python scripts/n8n_line_to_telegram.py`——把 4 支流程的 LINE Push 節點換成 Telegram 節點。
   08-19 `--dry-run` 驗過會改到哪 4 支，但實際寫入被權限擋下來沒跑成
2. 跑完對「KEIS 情資週報」做一次 deactivate + activate（它是這 4 支裡唯一 active 的）
3. 把**靜默失敗巡邏／KEIS 待聯絡提醒／KEIS 心跳檢查**三支重新啟用
4. 加防吵：靜默失敗巡邏「同一種錯 24 小時內只報一次」、心跳「只在狀態轉變時報」。
   現在兩支都是每輪重報，開回去會很吵（這是啟用的前置，不是省額度用的）

---

## 🔴 三條永久紅線（動之前先看 `incidents.md`）

1. **Railway `EXECUTIONS_DATA_SAVE_ON_SUCCESS` 一律保持 `all`**
   改 `none` 會讓所有含 Wait 節點的流程無聲死掉（煞車 10 分鐘就是 Wait）。
   空間不夠的正確解法是調降 `MAX_AGE`，不是關掉紀錄。
   清執行紀錄一律用 **TRUNCATE**，絕不用 API 批次 DELETE（08-09 停機經過見 `incidents.md`）。
2. **AI 回傳壞掉時寧可不發，絕不硬發**
   保底邏輯只要「讓流程繼續」而不管「送出去的東西對不對」，就是在把故障洗成看起來成功的樣子。
   對外送出的東西一律要有形狀檢查。
3. **KEIS `show_in_web=false` 的案子絕對不發**（屋主/門市沒同意網路露出）。擋在 `篩選候選` 最上游。

**另外兩條通則**：監控不准住在 n8n 裡（見廣告發文看門狗）；改完一定要在「真正在跑的環境」實地驗一次。

---

## 系統現況（2026-08-24，用 Public API 實地核對過）

| 線 | 狀態 |
|---|---|
| 線 A 掃描發文／線 B 下架偵測／線 C 重發輪替 | 🟢 08-24 全部重新啟用（deactivate+activate 跑過）；**但 FB 權杖死了，發不出去**，見上面 🔴 |
| 系統錯誤告警／工作回報查詢／搶單通知／簽到通知（都改寫系統日誌，不推播） | 🟢 已開 |
| 煞車（停）／LINE 指令分流器／圖片分流器／客戶建檔器／行事曆建立器／戰果查詢／查專員電話 | 🟢 已開（LINE 觸發，沒時鐘）|
| 情資週報 | 🟢 已開，「情資」關鍵字回覆能用；推播分支等 Telegram 遷移 |
| 靜默失敗巡邏／KEIS 待聯絡提醒／KEIS 心跳檢查 | ⚪ 已停，**Telegram 遷移完就開回來** |
| 廣告發文看門狗／自動簽到／市場週報／行情看板更新 | ⚪ 已停（沒動）|
| 公買搶單（門市電腦 grab.py） | 🟢 08-24 重開＋開機捷徑放回 `shell:startup`（之前被移出忘記放回）|
| YC 建檔器 v2/v3／YC 發文線／文案重產器／LINE 新聞推播 | ⚪ 已停（舊版，沒動）|

「LINE 推播 → Notion 系統日誌」的架構細節見 `reference.md`。

---

## ▶ 下次開工從這裡接

### 0.1 守門員誤擋：第 10 個 bug 已修（08-10），穩定兩週沒再誤擋，經過見 `incidents.md`

⚠️ 改守門員只能「補真出處」不能「放寬檢查」；廠房／土地「格局空白」是正常的，別為此放寬。

### 0.15 Codex 文案服務**已接線**（`scripts/codex-copy/`，取代 Gemini 免費配額）

⚠️ **不穩**：cloudflared 通道每次重開網址都會變，斷過好幾次害線 A 空跑（見 `incidents.md`）。
開機捷徑已放回 `shell:startup`，重開機會自動接通道回寫 n8n，但**通道本身斷掉沒有退路**，
斷線頻率還要觀察。

**已決定不做**：煞車縮短、文案純套版、Google Cloud 開帳單、交叉審核換 Codex（理由見 `reference.md`）。

### 0.5 Gemini 免費配額每天 20 次（每個模型）——08-14 起壓力變小

線 A「產文案」已改走 Codex（見 0.15），不再吃這桶。現在共用 `flash-lite` 的只剩線 C 6 班
＋ LINE 三個指令。（交叉審核走 `flash`，是另一桶，不相干。）細節見 `reference.md`。

### 0.8 KEIS 被砍的欄位（`images`／`age`／`school_info`…）已修復，**先別改回去**——見 `reference.md`

### 1. AG1911938 鳳山鳳松路透天：過兩天再看

KEIS 資料完整、編號有效（1580 萬／空屋），但**官網真的還沒有**（編號差 1 號的 AG1911937 有
→ 不是搜尋壞掉）。只能看「連續幾天都查無 = 大概真的不上網」。
⚠️ 坪數**不是填錯，別去問店家**：有加蓋，單價 110 萬/坪 是拿登記坪當分母才顯得離譜。

### 2. 兩段還沒被真實事件走完的流程（都各自驗過了，等自然發生）

- **FB 補救分支**：等 FB 真的打嗝。收到「❌ 廣告沒發出去」就看該班 `判斷補救方式` 的 `mode`
- **線 C 的 FB 發新文＋刪舊文**：程式路徑已就緒但**還沒被真實事件走完**。EG0506900
  （1298→1180）`要重發` 已手動勾上，等下一班線 C 跑完看有沒有走完整條路徑，沒有就記錄卡點

### 3. 公開網頁在 Cloudflare Pages（發布鏈路見 `reference.md`）

- 三合一計算機 https://yc-tools.pages.dev/yc-calc/
- 行情看板 https://yc-tools.pages.dev/kh-market/

⚠️ **`qingan3-calc` 還是 404**：GitHub 那邊的障礙已解除，問題回到本質——**沒有本機原始檔**，
要救得先找到原始檔才動得了。

---

## 其他待辦

- **Notion「來源連結」欄已刪（08-05）**：存的是會過期的 houseol 網址。引用全改指 `永慶官網連結`。
  **不要再加回來**，理由見 `reference.md`
- **Notion v2 舊資料**：YC1868705 還在、不在 KEIS，線 B 掃到略過；`永慶官網連結` 已補上
- **公買搶單（08-14）**：`EXCLUDE_TYPES` 加「土地」；拿掉第三個帳號許育禎，現在兩帳號共 14 配額；
  `EXCLUDE_DISTRICTS` 排除美濃/六龜/岡山/路竹/大寮/橋頭/杉林/燕巢/林園/梓官；
  `--record`（只掃描記錄不申請）給空窗天用——空窗 ≥7 天回來前先跑一次補紀錄再開 `--watch`
- **公買搶單心跳假警報（08-24）已拔掉**：`KEIS_HEARTBEAT_WEBHOOK` 指的「KEIS 心跳檢查」
  workflow 08-12/13 起就永久停用（監控搬去 grab.py 本地計時器＋Telegram 直推），
  再指過去只會一直 404、每 2 小時噴一次誤導性「n8n 可能掛了」。已從 repo 與桌面兩份 `.env`
  移除該變數，重啟生效。開機捷徑（`run - 捷徑.lnk`）同天從 `_停用_開機捷徑` 資料夾移回
  `shell:startup`，之前不知道何時被移出、沒人放回去。
- **未來候補**：多開一個 IG 帳號專發廣告。使用者要先自己把新 IG 轉商業帳號、綁粉專＋開權限，
  之後才接 n8n（IG 是兩段式 container→publish）。目前卡在帳號還沒建，不用主動催
