# STATUS — 現在的狀態與接下來要做的事

> **這份只放「現況」和「待辦」。完成的項目直接刪掉，不留歷史。**
> 查表（入口/credential/Notion 欄位/指令/工具清單）→ `reference.md`
> 事故經過與血淚史 → `incidents.md`
> 線上真正在跑什麼 → `n8n-live.md`（n8n_sync.py 產的，**兩邊打架信這份**）
> 外部 AI 工具情報（評估過的／已排除的，不用重評）→ `ai-toolbox.md`

最後更新：2026-08-06 ｜ 使用者：薛力瑜（永慶不動產 博愛凱璿加盟店）

---

## 🔴 開工第一件事：GitHub 帳號停權中（2026-07-30 11:47 起）

`git push` / `gh pr merge` 一律回 403 `Your account was suspended`。
**不要浪費時間重試推送，收工一律只做本機 commit，跳過 push/PR，等帳號恢復再一次補推。**

- **未推的東西**：本機分支 `claude/multi-store-price-fix-0730` 有 78 個 commit 未推（2026-08-06 計）；
  GitHub 上 **PR #161 開著未合**。恢復後：`git push` → 合 PR #161 → 把後續 commit 一起併進 main
- **離線備份**：`桌面\asce-備份-20260730.bundle`｜**申訴**：07-30 已送工單，等回信，不用重複送
- 已設每天自動檢查帳號有沒有恢復；排程與桌面工具**完全不依賴 GitHub**，停權期間照跑
- 觸發原因與往後的 PR 節奏 → `incidents.md`

---

## 🔴 三條永久紅線（動之前先看 `incidents.md`）

1. **Railway `EXECUTIONS_DATA_SAVE_ON_SUCCESS` 一律保持 `all`**
   改 `none` 會讓所有含 Wait 節點的流程無聲死掉（煞車 10 分鐘就是 Wait）。
   空間不夠的正確解法是調降 `MAX_AGE`，不是關掉紀錄。
2. **AI 回傳壞掉時寧可不發，絕不硬發**
   保底邏輯只要「讓流程繼續」而不管「送出去的東西對不對」，就是在把故障洗成看起來成功的樣子。
   對外送出的東西一律要有形狀檢查。
3. **KEIS `show_in_web=false` 的案子絕對不發**（屋主/門市沒同意網路露出）。擋在 `篩選候選` 最上游。

**另外兩條通則**：監控不准住在 n8n 裡（見廣告發文看門狗）；改完一定要在「真正在跑的環境」實地驗一次。

---

## 系統現況

| 線 | 狀態 |
|---|---|
| 線 A 掃描發文（09/11/13/15/17/19）／線 B 下架偵測（09:30、22:00）／線 D KEIS 同步 | 🟢 active |
| 線 C 重發輪替（09:30～19:30 每 2 小時）| 🟢 active；**FB 發/刪那段還沒真的跑過** |
| 煞車（停）／ LINE 指令分流器 ／ 靜默失敗巡邏 | 🟢 active |
| 系統錯誤 LINE 告警 | 🟢 active（**必須保持 active**，否則不轉發）|
| 廣告發文看門狗（桌面，n8n 外，每小時）／公買搶單／自動簽到／買方配案排程 | 🟢 上線中 |

---

## ▶ 下次開工從這裡接

### 0. ★ 文案交叉審核 2026-08-06 已上線，**觀察前幾班有沒有誤擋**

16 篇真實已發布文案驗證：誤擋 0、真抓到 1（YG0089924 粉專文案混進印地文亂碼 `पसर`，
Notion 與粉專都已修正）、SKIP 1（審核員自己壞掉→依設計放行）。全庫 83 筆只有那 1 篇有亂碼。
收到「交叉審核擋下」的 LINE 告警就去看理由站不站得住腳，站不住就回頭調 `copy_review_nodes.json` 的 prompt。

**線上第一班已過**：08-06 17:00（執行 5211）AG1941865 → `OK`，沒誤擋。往前四班沒走到審核（該班無候選案件，不是被跳過）。

⚠️ **不急但要知道**：`verify_copy_review.py` 餵的事實跟正式線**不同源**——測試用 KEIS 賣點原文 ＋ `school_info`；
正式線用 `stripTopic` 刪減後的賣點（官網沒學區就砍學區句）＋ 官網正則抓的學區。因為產文案也是拿刪減後的資料寫的，
文案跟審核員看到的還是對得上，**方向安全**；但「16 篇零誤擋」的保真度不是 100%，真正的驗收是線上這幾班。

### 0.5 🔴 Gemini 免費配額每天只有 20 次（每個模型），系統本來就貼著上限在跑

線 A 6 班 + 線 C 6 班 + LINE 三個指令共用 `flash-lite`，已接近 20，**使用者多打幾次「客戶」
「行事曆」就會讓廣告線在 `產文案` 靜靜失敗**。（交叉審核走 `flash`，是另一桶 20 次，不相干。）
根治：分散到不同模型，或開 Google Cloud 帳單轉付費（月費不到一杯飲料）。細節見 `reference.md`。

### 0.8 KEIS 被砍的欄位（`images`／`age`／`school_info`…）2026-08-04 都回來了，**先別改回去**

現在的繞道做法比較耐打，KEIS 砍過一次就會砍第二次。詳見 `reference.md`。

### 1. ★ 守門員誤擋要修（2026-07-30 診斷完，使用者說「過兩天再處理」）

三件誤判的成因與改法（含要動哪一行）整理在 `incidents.md` 最上面那則 2026-07-30。
還沒動手改。被擋的兩件躺在 Notion 草稿，要發就改「待發」。

### 2. AG1911938 鳳山鳳松路透天：過兩天再看

KEIS 資料完整、編號有效（1580 萬／空屋），但**官網真的還沒有**（編號差 1 號的 AG1911937 有
→ 不是搜尋壞掉）。只能看「連續幾天都查無 = 大概真的不上網」。
⚠️ 坪數**不是填錯，別去問店家**：有加蓋，單價 110 萬/坪 是拿登記坪當分母才顯得離譜。

### 3. 兩段還沒被真實事件走完的流程（都各自驗過了，等自然發生）

- **FB 補救分支**：等 FB 真的打嗝。收到「❌ 廣告沒發出去」就看該班 `判斷補救方式` 的 `mode`
- **線 C 的 FB 發新文＋刪舊文**：程式路徑已就緒但**還沒被真實事件走完**。2026-08-05 起線 B
  看到 KEIS 掛「降價」標籤就自動勾 `要重發`，線 C 沿用舊文案只換價＋插一行「🔻 降價 X 萬」，
  發完回寫 Notion 總價與 KEIS `adcase_price`／FB 連結。**首件 EG0506900（1298→1180）等點頭才勾**

### 4. 買方配案剩下的小事

- **2026-08-06 新增**：客戶被移出房地資料夾後，manifest／看板現在會自動清掉舊資料（`manifest.prune_group`，
  跑某群組前先跟房地當下名單比對）。已手動清過一次舊殘留、桌面 code 已同步；下次排程跑時
  再確認 prune 有正常觸發。
- **⏰ 2026-08-11 後刪掉 `C:\Users\user\_待刪_20260804_買方配案舊登入態\`（4.8 GB）**
  兩份停用的 Chrome 登入態，08-04 搬出來但沒刪、觀察一週。刪前看 `daily_run.log` 有沒有正常跑。
  徵兆是 LINE「🔑 房地要重新登入」→ 掃一次 QR Code 即可，不用搬回來。細節見記憶 buyer-match-chrome-profiles。
- 車位型態（平面式/機械式）還沒對應，目前只做到「有/無車位」
- 看板三個入口（localhost:5001／桌面 `配案看板.html`／手機 artifact）**版面已整併成一份**
  `build_static_view.py`（08-05）。5001 那支以前自己寫一份條列式版面，改版時漏掉、
  使用者天天看的就是它。**以後不要在 `webview_server.py` 裡寫 HTML。**
- 手機 artifact（`c30bd39a-…`）**要人工重新發布**才會更新，排程只會重產桌面那份
- 電腦上「點連結原地預覽」在 5001 和桌面單檔都有；artifact 包在 claude.ai 的 iframe 裡
  會被攔截，只能開新分頁或按「複製連結」

### 5. 公開網頁已搬到 Cloudflare Pages（2026-08-05 上線）

**新網址（給客戶／自己用的）**
- 三合一計算機 https://yc-tools.pages.dev/yc-calc/
- 行情看板 https://yc-tools.pages.dev/kh-market/

原因：帳號停權**連 GitHub Pages 一起關掉**，`github.io` 底下三個站對外全部 404
（`git ls-remote` 讀取也 403）。「情資沒更新」不是 bdinfo 壞掉——每週二照樣算好寫檔，是推不上去。

現在的鏈路：週二 09:55 bdinfo 產完 `dist/data/intel/latest.json` → **當場叫 `update.py --deploy-only`
發布**（不用等 13:37）→ 每天 13:37 `KH-Market-AutoUpdate` 再發一次保底 → 發完**真的去線上抓一次
比對**，不符或 404 就推 LINE（同故障 3 天只吵一次，狀態存 `publish_state.json`）。
GitHub 仍是次要通道，帳號恢復當天會自動補推積著的 commit。手動重發：`python update.py --deploy-only`

⚠️ **`qingan3-calc` 沒有本機原始檔、GitHub 讀取也被擋，只能等帳號恢復才搬得動**（現在還是 404）。

---

## 🔴 未解：桌面 grab.py 跟 repo 兩邊各有對方沒有的東西（2026-08-04 收工發現）

**不要單方向複製，會毀掉真的東西。** 差 151 行的雙向分岔：
桌面獨有＝`WINDOW_SIZE=70`、縣市空白當高雄放行、漏接偵測（08-03 加的，**現行規則**）；
repo 獨有＝08-01 起 6 個 commit（心跳直推 LINE／總帳備份／帳號健檢／開盤守門／API 體檢，**從沒上線**）。
桌面版備份在 `backup/keis-desktop-snapshot-20260804/grab.py`（gitignore）。
**要怎麼收**：桌面獨有三項合進 repo 版 → 覆蓋桌面 → 實跑一天確認。
⚠️ 動之前確認沒有別的 session 正在改 grab.py。

## 其他待辦

- **Notion「來源連結」欄已刪（2026-08-05）**：存的是會過期的 houseol 網址（刪除前 72 筆全部 404）。
  引用全改指 `永慶官網連結`（線 B 二次確認／`publish.py`／yc-ad skill）。**不要再加回來**，理由見 `reference.md`
- **Notion v2 舊資料**：YC1868705 還在、不在 KEIS，線 B 掃到略過；它的 `永慶官網連結` 已補上
- **LINE「戰果」最終驗收**：需使用者拿手機打一次（需真實 replyToken，模擬不了）
- **n8n 有兩個同名「LINE 指令分流器」**，舊的已停用未刪，跑幾天沒問題後刪掉舊的
- **公買搶單對帳未驗的一半**：周珈伊 7 筆需登入她帳號才能核（密碼類使用者自己登入）
- **未來候補**：多開一個 IG 帳號專發廣告。使用者要先自己把新 IG 轉商業帳號、綁粉專＋開權限，
  之後才接 n8n（IG 是兩段式 container→publish）。目前卡在帳號還沒建，不用主動催
